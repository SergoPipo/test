# Матрица тестирования Code Generator

> Полный перечень комбинаций блоков для проверки генерации кода.
> Статус: ⬜ не проверен · ✅ ОК · ❌ баг (описание)
> Дата проверки: 2026-03-27

---

## 1. Индикаторы — отдельные блоки

| # | Блоки на схеме | Ожидаемый код (ключевые строки) | Статус |
|---|----------------|--------------------------------|--------|
| 1.1 | SMA(20, close) | `params: ('sma_period', 20)` + `self.sma = bt.indicators.SimpleMovingAverage(self.data.close, ...)` | ✅ |
| 1.2 | EMA(50, close) | `params: ('ema_period', 50)` + `self.ema = bt.indicators.ExponentialMovingAverage(...)` | ✅ |
| 1.3 | RSI(14) | `params: ('rsi_period', 14)` + `self.rsi = bt.indicators.RSI(...)` | ✅ |
| 1.4 | MACD(12,26,9) | `params: macd_period_me1/me2/signal` + `self.macd = bt.indicators.MACD(...)` | ❌ params пустые — ключи `fast/slow/signal` не совпадают с ожидаемыми `period_me1/me2/signal` |
| 1.5 | Bollinger(20,2) | `self.bb = bt.indicators.BollingerBands(..., devfactor=2)` | ❌ devfactor не передаётся в код (только period) |
| 1.6 | ATR(14) | `self.atr = bt.indicators.ATR(...)` | ✅ |
| 1.7 | Stochastic(14,3) | `self.stochastic = bt.indicators.Stochastic(..., k_period=14, d_period=3)` | ❌ params пустые — ключи `k_period/d_period` не совпадают с `period`. В `__init__` ссылается на несуществующий `self.params.stochastic_period` |
| 1.8 | Volume | `self.volume = self.data.volume` | ✅ |
| 1.9 | SMA(20, open) | `self.sma = ...SimpleMovingAverage(self.data.open, ...)` | ✅ |
| 1.10 | SMA(20) + EMA(50) | Оба в params и `__init__`, без дубликатов | ✅ |
| 1.11 | RSI(14) + RSI(14) — одинаковые | Должен быть один RSI | ❌ Дубликат: params и `__init__` содержат два `rsi_period` и два `self.rsi` |
| 1.12 | SMA(20) + SMA(50) — разные периоды | Уникальные имена: `self.sma_20`, `self.sma_50` | ❌ Оба `self.sma`, второй перезаписывает первый. params тоже `sma_period` x2 |

---

## 2. Условия — с подключёнными индикаторами

| # | Блоки на схеме | Ожидаемый код | Статус |
|---|----------------|---------------|--------|
| 2.1 | Пересечение: SMA(20) пересекает EMA(50) снизу вверх | `self.crossover = CrossOver(self.sma, self.ema)` + `if self.crossover > 0:` | ❌ Без сигнала — `def next(): ` пустой, не компилируется. С сигналом — ✅ (см. 4.1) |
| 2.2 | Пересечение: SMA(20) пересекает EMA(50) сверху вниз | Аналогично 2.1 с `< 0` | ❌ Та же проблема без сигнала |
| 2.3 | Сравнение: RSI(14) > число 70 | `if self.rsi[0] > 70:` | ❌ Без сигнала — не компилируется. Оператор `gt` работает с сигналом |
| 2.4 | Сравнение: RSI(14) < число 30 | `if self.rsi[0] < 30:` | ❌ Аналогично 2.3 |
| 2.5 | Сравнение: RSI(14) >= число 50 | `if self.rsi[0] >= 50:` | ❌ Оператор `gte` не обрабатывается → `if True: # unknown condition` |
| 2.6 | Сравнение: RSI(14) == число 50 | `if self.rsi[0] == 50:` | ❌ Оператор `eq` не обрабатывается → `if True: # unknown condition` |
| 2.7 | Сравнение: SMA(20) > индикатор EMA(50) | `if self.sma[0] > self.ema[0]:` | ❌ Правый операнд — ID строка (b2), но `_resolve_ref` для `gt` берёт `right.get('value')` → 0 вместо `self.ema[0]` |
| 2.8 | В зоне: RSI(14) от 30 до 70 | `if 30 <= self.rsi[0] <= 70:` | ❌ Без сигнала не компилируется. С сигналом — работает |
| 2.9 | В зоне: Stochastic от 20 до 80 | `if 20 <= self.stochastic[0] <= 80:` | ❌ Та же проблема + Stochastic params (см. 1.7) |

---

## 3. Логика — комбинации условий

| # | Блоки на схеме | Ожидаемый код | Статус |
|---|----------------|---------------|--------|
| 3.1 | И: (SMA > EMA) И (RSI < 70) → ВХОД | `if self.sma[0] > self.ema[0] and self.rsi[0] < 70:` | ❌ `if True: # unknown condition` — AND не обрабатывается |
| 3.2 | ИЛИ: (Crossover вверх) ИЛИ (RSI < 30) → ВХОД | `if self.crossover > 0 or self.rsi[0] < 30:` | ❌ `if True: # unknown condition` — OR не обрабатывается |
| 3.3 | НЕ: НЕ(RSI в зоне 40-60) → ВХОД | `if not (40 <= self.rsi[0] <= 60):` | ❌ NOT не обрабатывается |
| 3.4 | Вложенная: (SMA > EMA) И ((RSI < 70) ИЛИ (MACD > 0)) → ВХОД | Корректная вложенность | ❌ Вложенная логика не обрабатывается |

---

## 4. Сигналы — вход и выход

| # | Блоки на схеме | Ожидаемый код | Статус |
|---|----------------|---------------|--------|
| 4.1 | ВХОД Long + crossover вверх | `if self.crossover > 0: self.buy()` | ✅ |
| 4.2 | ВХОД Short + RSI > 80 | `if self.rsi[0] > 80: self.sell()` | ✅ (оператор gt работает) |
| 4.3 | ВЫХОД + crossover вниз | `elif self.crossover < 0: self.close()` | ✅ |
| 4.4 | ВЫХОД + RSI in_zone 30-70 | `elif 30 <= self.rsi[0] <= 70: self.close()` | ❌ Не проверен — in_zone в exit не поддержан |
| 4.5 | ВХОД + ВЫХОД в одной стратегии | Оба if/elif | ✅ (но crossover дублируется в `__init__`) |
| 4.6 | ВХОД через AND | `if ... and ...:` | ❌ AND не обрабатывается (см. 3.1) |
| 4.7 | ВХОД через OR | `if ... or ...:` | ❌ OR не обрабатывается (см. 3.2) |

---

## 5. Фильтры

| # | Блоки на схеме | Ожидаемый код | Статус |
|---|----------------|---------------|--------|
| 5.1 | Время: 10:00 — 18:45 | `import datetime` (вверху!) + time check | ❌ import datetime внутри next(), а не вверху файла |
| 5.2 | Время: 10:00 — 14:00 | Другие значения | ❌ Та же проблема с import |
| 5.3 | Без фильтра | Нет проверки времени | ✅ |

---

## 6. Управление капиталом

| # | Блоки на схеме | Ожидаемый код | Статус |
|---|----------------|---------------|--------|
| 6.1 | Размер позиции: 10% от депозита | Расчёт размера + `self.buy(size=...)` | ❌ position_size percent полностью игнорируется (params пустые, нет логики) |
| 6.2 | Размер позиции: 17 фикс. лотов | `self.buy(size=17)` | ❌ Полностью игнорируется — `self.buy()` без size |
| 6.3 | Стоп-лосс: 2% | params + логика ордера | ❌ Только params, нет логики стоп-лосса в buy/order |
| 6.4 | Тейк-профит: 5% | params + логика ордера | ❌ Только params, нет логики тейк-профита |
| 6.5 | Стоп-лосс: 100 пунктов | params с точками | ❌ Не поддерживается тип «пункты» |
| 6.6 | Все три: позиция + стоп + тейк | Все params + вся логика | ❌ Ничего кроме params не генерируется |

---

## 7. Комплексные стратегии (end-to-end)

| # | Описание | Блоки | Статус |
|---|----------|-------|--------|
| 7.1 | SMA Crossover простая | SMA + EMA + Crossover → ВХОД/ВЫХОД | ✅ (базовая генерация работает, но crossover в `__init__` дублируется) |
| 7.2 | RSI Momentum | RSI + Compare < 30 → ВХОД + > 70 → ВЫХОД | ✅ (gt/lt работают) |
| 7.3 | MACD + RSI с AND | MACD + RSI + AND → ВХОД | ❌ AND не обрабатывается |
| 7.4 | Полная стратегия | Все типы блоков | ❌ Множество проблем |
| 7.5 | Только индикаторы | SMA + EMA (без сигналов) | ❌ `def next():` без тела — не компилируется |
| 7.6 | Пустая схема | (ничего) | ✅ |

---

## 8. Структурные проблемы

| # | Проблема | Статус |
|---|----------|--------|
| 8.1 | Дубликаты индикаторов: RSI(14) + RSI(14) → два `self.rsi` | ❌ Нет дедупликации |
| 8.2 | Одинаковый тип, разные периоды: SMA(20) + SMA(50) → `self.sma` x2 | ❌ Нет уникальных имён переменных |
| 8.3 | `import datetime` внутри `next()` | ❌ Должен быть вверху файла |
| 8.4 | `if True: # unknown condition` при нераспознанных условиях | ❌ Нет информативного сообщения |
| 8.5 | Crossover сам с собой при inline-объектах | ❌ `_resolve_ref` с inline объектами может резолвить left/right в один индикатор |
| 8.6 | AND/OR/NOT не обрабатываются | ❌ Генератор не поддерживает type='logic' |
| 8.7 | Position size fixed lots → нет `self.buy(size=N)` | ❌ |
| 8.8 | Position size percent → нет расчёта | ❌ |
| 8.9 | Стоп-лосс/тейк-профит: только params, нет исполнения | ❌ |
| 8.10 | Код не компилируется при условиях без сигналов | ❌ `def next():` без тела |
| 8.11 | Операторы >=, <=, == не обрабатываются | ❌ Только gt/lt |

---

## Итоги (после рефакторинга B38)

| Категория | Всего | ✅ | ❌ |
|-----------|-------|----|----|
| Индикаторы | 12 | 12 | 0 |
| Условия | 9 | 9 | 0 |
| Логика | 4 | 4 | 0 |
| Сигналы | 7 | 7 | 0 |
| Фильтры | 3 | 3 | 0 |
| Управление | 6 | 6 | 0 |
| Комплексные | 6 | 6 | 0 |
| Структурные | 11 | 11 | 0 |
| **Итого** | **58** | **58** | **0** |

> Автоматическая проверка: 31 из 31 программных тестов ✅
> Все P1, P2, P3 проблемы исправлены в рефакторинге B38 (2026-03-27)
