# E2E Test Plan — Sprint 7

> **Заполняется QA-агентом на W0 ДО написания тестов** (правило памяти `feedback_e2e_testing.md`).
> Этот файл — **скелет** с разделами по каждой задаче. QA дополняет конкретные сценарии: предусловия, шаги, ожидаемый результат.

## Источники

- `Спринты/ui_checklist_s5r.md` — текущий UI-чеклист (база).
- `Sprint_7/sprint_design.md` — функциональность задач S7.
- `Sprint_7/ux/*.md` — UX-макеты W0 (источник истины для UI-проверок).
- `Sprint_7/execution_order.md` — Cross-DEV contracts (для проверки backend интеграций).

## Тестовый аккаунт

`sergopipo` (правило памяти `user_test_credentials.md`; пароль у заказчика).

## Регрессия

Baseline после Sprint_6_Review: **119 passed / 0 failed / 3 skipped**.
После S7: ≥ 119 + ≥ 15 новых (по 1+ на каждую feature-задачу: 7.1, 7.2, 7.3, 7.6, 7.7, 7.8, 7.9, 7.12, 7.13, 7.14, 7.15, 7.16, 7.17, 7.18, 7.19).

## Расположение тестов

`Develop/frontend/e2e/sprint-7/<feature>.spec.ts`

---

## Сценарии по задачам

> Каждый блок ниже QA дополняет на W0 конкретными сценариями перед написанием тестов.

### 7.1 Версионирование стратегий (BACK2 + FRONT2)

- [ ] **TBD by QA** — сценарий «создал стратегию → отредактировал → создал версию → откатил».
- [ ] **TBD by QA** — сценарий «история показывает все версии с метаданными (created_at, created_by, comment)».
- [ ] **TBD by QA** — сценарий «откат не теряет текущую версию (создаётся новая из старой)».

### 7.2 Grid Search (BACK1 + FRONT2)

- [ ] **TBD by QA** — сценарий «параметры → запуск → matrix отображается с heatmap».
- [ ] **TBD by QA** — сценарий «hard cap N комбинаций — пользователь получает предупреждение».

### 7.3 Экспорт CSV/PDF (BACK2)

- [ ] **TBD by QA** — сценарий «бэктест → экспорт CSV скачивается, открывается в Excel/Numbers».
- [ ] **TBD by QA** — сценарий «бэктест → экспорт PDF скачивается, отображается».

### 7.6 Инструменты рисования (FRONT1)

- [ ] **TBD by QA** — сценарий «провёл трендовую линию → перезагрузил → линия осталась».
- [ ] **TBD by QA** — сценарий «горизонтальный уровень переносится drag&drop».
- [ ] **TBD by QA** — сценарий «прямоугольная зона удаляется через контекстное меню».

### 7.7 Дашборд-виджеты (FRONT1 + FRONT2)

- [ ] **TBD by QA** — сценарий «виджет баланса показывает корректное значение + sparkline».
- [ ] **TBD by QA** — сценарий «health indicator реагирует на состояние CB (срабатывание → красный)».
- [ ] **TBD by QA** — сценарий «мини-график активной позиции отображается».

### 7.8 First-run wizard (FRONT2 + BACK2)

- [ ] **TBD by QA** — сценарий «новый юзер → wizard показывается → 5 шагов → completed».
- [ ] **TBD by QA** — сценарий «после complete не показывается повторно (`wizard_completed_at` ≠ null)».
- [ ] **TBD by QA** — сценарий «нельзя пропустить дисклеймер без галки».

### 7.9 Backup/restore (OPS + BACK1)

- [ ] **TBD by QA** — smoke сценарий «бэкап создаётся по cron».
- [ ] **TBD by QA** — сценарий «восстановление через CLI: `python -m app.cli.restore --from <path>`».
- [ ] **TBD by QA** — сценарий «ротация: keep 7 копий».

### 7.12 NS singleton (BACK2)

- [ ] **TBD by QA** — регресс: уведомления продолжают приходить по всем 13 типам после рефакторинга DI.

### 7.13 5 event_type (BACK1)

- [ ] **TBD by QA** — сценарий: trade_opened → notification приходит in-app + Telegram + Email.
- [ ] **TBD by QA** — сценарий: partial_fill → notification приходит.
- [ ] **TBD by QA** — сценарий: order_error → notification приходит.
- [ ] **TBD by QA** — сценарий: all_positions_closed → notification приходит.
- [ ] **TBD by QA** — сценарий: connection_lost / restored → notifications приходят.

### 7.14 Telegram inline-кнопки (BACK2)

- [ ] **TBD by QA** — сценарий «клик `Открыть сессию` → редирект на /sessions/{id}».
- [ ] **TBD by QA** — сценарий «клик `Открыть график` → редирект на /chart?ticker=...».

### 7.15 WS карточки сессий (BACK1 + FRONT1)

- [ ] **TBD by QA** — сценарий «открыта вкладка Торговля → новая сделка → карточка обновилась без перезагрузки и без polling».
- [ ] **TBD by QA** — сценарий «WS отвалился → клиент переподключился (тест reconnect)».

### 7.16 Интерактивные зоны + аналитика (FRONT1)

- [ ] **TBD by QA** — сценарий «hover на зону → выделение белым контуром».
- [ ] **TBD by QA** — сценарий «клик на зону → панель деталей сделки».
- [ ] **TBD by QA** — сценарий «вкладка Обзор → гистограмма P&L отображается с легендой».
- [ ] **TBD by QA** — сценарий «вкладка Обзор → donut Win/Loss/BE отображается с числами».

### 7.17 Фоновый бэктест (BACK1 + FRONT1)

- [ ] **TBD by QA** — сценарий «клик `в фоне` → toast → бейдж в шапке инкрементируется».
- [ ] **TBD by QA** — сценарий «модалка остаётся открытой после клика `в фоне`».
- [ ] **TBD by QA** — сценарий «несколько фоновых параллельно — все в dropdown с прогрессом».
- [ ] **TBD by QA** — сценарий «бэктест завершился → status:'done' в WS → бейдж декрементируется».

### 7.18 / 7.19 AI-команды (BACK2 + FRONT1)

- [ ] **TBD by QA** — сценарий «ввёл `/` → dropdown показывается с 5 командами».
- [ ] **TBD by QA** — сценарий «выбрал `/chart EURUSD` → enriched prompt отправлен → AI ответил с контекстом тикера».
- [ ] **TBD by QA** — сценарий «`/strategy 123` где id принадлежит другому юзеру → 403».

---

## Шаги верификации

> Правило памяти `feedback_e2e_testing.md` — 5 шагов цикла:
>
> 1. Описание (ВЫШЕ — заполнено QA до написания тестов).
> 2. Инфраструктура — `preflight_checklist.md` пройден.
> 3. Написание тестов — `Develop/frontend/e2e/sprint-7/*.spec.ts`.
> 4. Запуск — `cd Develop/frontend && npx playwright test`.
> 5. Верификация — passed/failed зафиксирован в `Sprint_7/arch_review_s7.md`.
>
> QA не имеет права отчитываться «тесты написаны» без раздела «тесты прогнаны» с цифрами.

## Критерий приёмки 7.11

- ≥ 119 baseline тестов passed (S6 Review baseline).
- ≥ 15 новых тестов passed (по 1+ на feature-задачу).
- 0 failed.
- skipped — только с указанием тикета `S7R-<SHORT-NAME>` и карточкой в `Sprint_8_Review/backlog.md`.
