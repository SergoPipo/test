# Sprint_8_Review — План завершения приёмки (acceptance execution plan)

**Дата:** 2026-07-26 · **Модель:** Opus 4.8 · **Автор:** Claude + заказчик (планирование в чате).

## Контекст

Полное код-ревью 2026-07 (P0+P1+auth-hardening+BE-TRAD-06) сведено в `s8r/bug-31-unified-codegen` (PR #7 `94721e8` + PR #8 `eba6427`). Открытых P1 нет. Осталось довести приёмку Sprint 8 до вердикта Gate → старт Sprint 9.

**Прогресс чеклиста** (`acceptance_checklist.md`): 53 ✅ / 0 багов / 57 ⬜ (Gate = 136 пунктов). Все 6 сквозных сценариев (Шаг 2) + Шаг 0/1 — зелёные. Остаток:
- **S8.1–S8.12** (UI-секции) — 52 пункта, 👆 UI-проверка;
- **S8.14, S8.17** — 3 пункта, 🤖 технические;
- **S8.15** roll-up + Финальный вердикт — 4 пункта.

## Решения заказчика

| Развилка | Решение |
|---|---|
| Кодовая база приёмки | **s8r/bug-31-unified-codegen** (консолидированный, то что пойдёт в прод) |
| Подход к UI-секциям S8.1–S8.12 | **Полностью я через Playwright** (+ скриншоты-evidence) |
| Перепроверка P1-затронутого | **Да, выборочно** (auth-cookie/WS + BE-TRAD-06 деньги/CB) |
| Креды AI/Telegram в стенд | **Сидировать из копии рабочей БД** (тот же ENCRYPTION_KEY; zero ручного ввода) |

## Фазы

### Ф0 — Изолированный стенд приёмки на s8r
1. Worktree Develop от `origin/s8r/bug-31-unified-codegen` (ветка `s8r-acceptance`).
2. Скопировать `.env` (целиком, не читая) → `DEBUG=true` (cookie Secure=false над http).
3. Снапшот рабочей БД через `sqlite3 .backup` (консистентно, WAL-safe) в изолированный файл; `DATABASE_URL` стенда → на копию.
4. **Деактивировать открытые торговые сессии в КОПИИ** (не в live!) — совпадает с drain-процедурой BE-TRAD-06 §7 + предотвращает авто-старт T-Invest-стримов.
5. T-Invest-стрим в стенде по умолчанию **придержан** (риск контеншена с live-инстансом по одному токену; для UI-секций живой стрим почти не нужен).
6. Backend (uvicorn) + frontend (vite/preview) на **отдельных портах** (напр. 8100/5273); live Develop не трогаем.
7. Логин под `sergopipo` (пароль не в чат — ввод в браузере стенда, либо тест-админ через CLI). Playwright нацелить на стенд.

### Ф1 — 🤖 Технические проверки (на s8r)
- Полный гейт: backend `pytest` (весь) + frontend `vitest`/`tsc`/`eslint` → passed/0.
- S8.10 security-заголовки; S8.13 единый `{items,total}`; S8.14 скорость/`@timed_event`/metrics; S8.16 `EVENT_MAP`↔`EVENT_TYPE_LABELS` + schemas↔api-types; S8.17 остаток (bandit/safety, coverage ≥80, docker build).

### Ф2 — UI-секции S8.1–S8.12 (Playwright, evidence → `screenshots/`)
S8.1 админ-меню · S8.2 `/admin/metrics` · S8.3 error-boundary · S8.4 статус-бейдж+фильтр · S8.5 4 виджета · S8.6 Telegram-кнопка (реальная отправка) · S8.7 типы уведомлений · S8.8 рисование · S8.9 AI→блоки (реальный AI, 1 мин. запрос) · S8.11 панель фон-бэктестов · S8.12 вкладки результатов.

### Ф2b — Выборочная перепроверка P1 (на s8r)
- **auth-hardening:** cookie-логин (3 HttpOnly), WS `auth_ok`, reload-бутстрап.
- **BE-TRAD-06:** виджет баланса (S8.5) + paper P&L (Сценарий 1/2) + CB в health-виджете — деньги двигаются корректно.

### Ф3 — Триаж багов
Найденные `[!]` → severity: critical/medium → фикс **test-first в цикле приёмки**; low/косметика → S9-backlog по согласованию. После фиксов — повтор гейта.

### Ф4 — Вердикт Gate
S8.15 roll-up → «Финальный вердикт» (**PASS** / **PASS WITH NOTES**) → обновить `acceptance_checklist.md` + `README` + `project_state` (S8R закрыт) → коммит в docs-репо → старт **Sprint 9**.

## Риски / зависимости
- **T-Invest контеншн:** копия БД + токен + активные сессии → авто-стримы стенда могут оборвать live-стримы (класс BUG-27). Митигация: деактивировать сессии в копии + придержать стрим стенда + вне торговых часов.
- **Логин:** копия содержит реальных юзеров, `/setup` не запустится → вход под sergopipo (пароль вне чата) или тест-админ через CLI.
- **Реальные внешние вызовы:** S8.6 — реальное Telegram-сообщение; S8.9 — реальный AI-запрос (расход токенов, ограничен минимумом).
- **Изоляция:** отдельные файл БД / порты / `.env`; live Develop (p1/wave2-backend) не затрагивается.
