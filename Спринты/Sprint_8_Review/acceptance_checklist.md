# S8 Acceptance Checklist (Sprint_8_Review)

> **Цель:** проверить текущую реализацию Sprint 8 на dev-окружении (start.sh, localhost). Это **главный артефакт Sprint_8_Review**.
>
> **Решение от 2026-05-14:** перевод в продуктив (Mac mini Docker, canary, watchdog) вынесен в отдельный **Sprint 9 "Перевод в продуктив"**, стартующий после Gate Sprint_8_Review.
>
> **Как пользоваться:**
> - Каждый пункт = чекбокс `[ ]`. Отметить выполнение: `[x]` (OK), `[!]` (баг), `[-]` (N/A).
> - Под пунктом — место для заметки. Описывай что увидел, чем не понравилось, какой шаг не работает.
> - Найденные баги собираются в секции «Найденные баги» внизу.
> - Когда всё пройдено — секция «Финальный вердикт» подписывается.
>
> **Окружение:**
> - `cd Develop && ./scripts/start.sh`
> - Backend: http://localhost:8000
> - Frontend: http://localhost:5173
> - Логин: `sergopipo` (пароль у заказчика)

**Дата старта:** _(заполни при старте)_
**Дата завершения:** _(заполни в конце)_
**Тестер:** sergopipo

---

## Шаг 0 — Pre-flight (5–10 минут)

- [x] `cd Develop && ./scripts/start.sh` — обе строчки `Backend: http://localhost:8000` и `Frontend: http://localhost:5173` появились, ошибок в терминале нет.
  > Заметка:

- [x] `http://localhost:5173` открылся в браузере.
  > Заметка:

- [x] DevTools (Cmd+Opt+I) → Console: **нет красных ошибок** (warnings ОК).
  > Заметка: в Console 4 ошибки, но 3 из 4 — dev-only артефакты, не баги продукта:
  > - `WebSocket ... closed before established` (×2, `/ws` и `/ws/trading-sessions/1`) — поведение React 18 StrictMode в dev: первый `useEffect` отменяется на unmount, второй коннект устанавливается стабильно. Backend log это подтверждает (`ws_connected` → `ws_subscribed channel=notifications:1,health`, держится). В production-сборке отсутствует.
  > - `T-Invest CDN 403` для `RU000A10DS74x160.png` (логотип облигации Сибур-Холдинг 001P-08) — у CDN нет логотипа для этой бумаги, фронт должен показать дефолтную плашку (UX-минорная вещь).
  > - **Реальный баг — sparkline 500** (SQLite `database is locked` при startup-конкуренции), вынесен в BUG-2. Симптом «CORS error / Status code: 500» на запросе sparkline — производный от 500 (CORSMiddleware не отдаёт заголовки в exception path).

- [x] Логин как `sergopipo` прошёл, попадаю на Dashboard.
  > Заметка:

- [x] DevTools → Network → запрос `/api/v1/health` ответил 200, в теле есть `tinvest_connected`, `scheduler_running`.
  > Заметка: проверено через `curl http://localhost:8000/api/v1/health` — `{"status":"ok","version":"0.1.0","database":"connected","cb_state":"ok","tinvest_connected":true,"scheduler_running":true,"scheduler_jobs":4}`. Все 4 ключевых поля присутствуют (status, database, tinvest_connected, scheduler_running).

**Если хоть один пункт Шага 0 не прошёл — стоп, сообщи мне.**

---

## Шаг 1 — Smoke по основным страницам (15–20 минут)

Открыть каждую страницу. Проверка: загружается без 500/JS-ошибок, видны данные (или понятное пустое состояние).

- [x] **Dashboard** — виджеты Health, Balance, Active Positions, Sparkline рендерятся.
  > Заметка:

- [x] **Strategy (список)** — список стратегий, кнопка «Создать» работает.
  > Заметка:

- [x] **Backtest** — страница запуска, выбор стратегии работает.
  > Заметка:

- [x] **Trading (paper/live сессии)** — список сессий загружается.
  > Заметка:

- [x] **Chart (график)** — открывается, свечки рендерятся, можно сменить таймфрейм.
  > Заметка:

- [x] **Account** — баланс, позиции, операции, налоги отображаются.
  > Заметка: 

- [x] **AI Chat** — окно открывается, можно отправить сообщение.
  > Заметка:

- [x] **Settings** — настройки уведомлений + broker settings грузятся.
  > Заметка:

- [x] **Admin Landing** (если ты admin) — открывается, есть ссылка на Plotly Dash.
  > Заметка:

---

## Шаг 2 — 6 сквозных сценариев (1 день)

Полные тексты сценариев — в [ui_checklist_s8.md секция S8.15](../ui_checklist_s8.md). Здесь — краткая шапка.

### Сценарий 1 — Новый пользователь → Wizard → стратегия → бэктест → paper → закрытие

- [x] Регистрация нового тестового пользователя (логин типа `test_acceptance_001`).
  > Заметка: Есть ошибка при вводе нового пользователя. Пишет просто ошибка создания пользователя.
  > 2026-05-29: BUG-12 заведён и зафиксирован — попытка создать `testuser` падала с 500 (UNIQUE constraint на username, уже сидел в БД от прежней сессии). Фикс: backend отдаёт 409, frontend показывает «Имя пользователя уже занято». Ожидается перепроверка с новым username (например, `test_acceptance_002`).

- [x] FirstRunWizard 4 шага проходятся: учётка → broker token → notifications → финиш.
  > Заметка:

- [x] Создаётся стратегия (Blockly или AI Chat).
  > Заметка: После создания стратегии блоков и переноса ее в текстовое значение, при нажатии на кнопку «Генерировать» я получил пайтинг от стратегии, но все блоки на схеме удалились. Текстовое представление стратегии тоже очистилось.

- [x] Запускается бэктест, дожидаюсь результатов, метрики корректны.
  > Заметка:

- [x] Запускается paper-сессия на результате бэктеста.
  > Заметка:

- [x] Paper-сессия закрывается через UI, статус меняется на `closed`.
  > Заметка:

### Сценарий 2 — Live торговля → мониторинг → закрытие

⚠️ **ВАЖНО:** Использовать SANDBOX-токен T-Invest, а не реальный счёт, пока не пройдёт W6b с TRADING_ENABLED guard.

- [x] Стратегия переведена в режим `live`.
  > Заметка:

- [x] Live-сессия запускается через UI.
  > Заметка:

- [x] Дожидаюсь хотя бы одного сигнала / ордера.
  > Заметка:

- [x] В Trading-странице видно открытую позицию / ордер.
  > Заметка:

- [x] Live-сессия закрывается вручную, позиции корректно отображаются как закрытые.
  > Заметка:

### Сценарий 3 — Background backtest (параллельные jobs)

- [x] Запускаю 2–3 бэктеста в фоне.
  > Заметка:

- [x] `BackgroundBacktestsBadge` в шапке показывает корректный счётчик.
  > Заметка:

- [x] После завершения панель auto-collapse через ~10 секунд.
  > Заметка:

- [x] Открываются результаты любого из них.
  > Заметка:

### Сценарий 4 — Grid Search heatmap

- [x] Запускается Grid Search (вариация параметров стратегии).
  > Заметка:

- [x] Дожидаюсь завершения, статус `done`.
  > Заметка:

- [x] Клик на `BackgroundBacktestsBadge` → modal с `GridSearchHeatmap` открывается.
  > Заметка:

- [x] Видны ячейки с цветовой шкалой P&L, hover показывает значения.
  > Заметка:

### Сценарий 5 — Admin + Plotly Dash /admin/metrics

- [x] `cd Develop/backend && .venv/bin/python -m app.cli.users grant_admin sergopipo` (выдать себе admin, если ещё нет). Исправлен путь модуля: `app.cli` → `app.cli.users` (см. BUG-7).
  > Заметка: выполнено 2026-05-27, SQL UPDATE прошёл, в логе: `User 'sergopipo' is now admin`.

- [x] Перезайти. В Sidebar появилась иконка щита (Shield).
  > Заметка: 2026-05-27 — после logout/login Sidebar показывает раздел «Администрирование».

- [x] AdminLandingPage открывается, ссылка на Plotly Dash присутствует.
  > Заметка: 2026-05-27 — открывается, в карточке «Активные торговые сессии» появилась колонка User (BUG-10 фикс).

- [x] `/api/v1/admin/metrics/` открывается, 4 графика рендерятся (signal→order, dashboard LCP, Telegram latency, backtest jobs rate).
  > Заметка: 2026-05-27 — все 4 графика рендерятся после фикса BUG-9 (path-based CSP для Plotly Dash). Cookie `access_token` выставляется на login (BUG-8).

### Сценарий 6 — Interactive zones + trade-row click

- [x] Открываются результаты закрытого бэктеста.
  > Заметка:

- [x] Переход на вкладку «Аналитика».
  > Заметка:

- [x] Hover на зоны equity-curve — tooltip появляется.
  > Заметка:

- [x] Клик на строку в таблице трейдов — открывается `TradeDetailsPanel`.
  > Заметка:

---

## Шаг 3 — проверка интерфейса по секциям (1–2 дня)

**Что это:** ты проходишь по работающему приложению и глазами/кликами проверяешь, что всё описанное реально работает. Кода писать не надо. Просто открываешь нужный экран, делаешь действие и сравниваешь с ожидаемым.

**Как пользоваться этим списком:**
- Иди сверху вниз. Для каждого пункта сделай действие и реши: ✅ или ❌.
- Если ✅ — поставь крестик: `- [ ]` → `- [x]`.
- Если ❌ или что-то странное — опиши в строке `📝 Заметки/баги:` под секцией (например: «S8.1: под обычным юзером пункт админки всё равно виден»).

**Условные обозначения:**
- 👆 **Ты кликаешь** — это видно глазами в браузере, проверяешь сам.
- 🤖 **Я проверю** — это «невидимые» проверки (ответы сервера, заголовки, прогоны тестов). Глазами не видно — просто скажи мне, и я выполню командами и впишу результат сюда.
- ✅ — так должно быть (хорошо). ❌ — так быть не должно (баг).

**Что нужно подготовить:**
1. Запущенные backend + frontend (если не подняты — скажи, помогу).
2. Два аккаунта: **админ** (первый зарегистрированный пользователь становится админом автоматически) и **обычный** пользователь.

---

> ### ✅ Результаты автопроверки 🤖 (2026-06-30, ветка `s8r/bug-31-unified-codegen`)
>
> Все «невидимые» пункты (🤖) проверены кодом и тестами — **ошибок не найдено, чинить нечего**:
> | Секция | Что проверено | Результат |
> |--------|---------------|-----------|
> | S8.1 / S8.2 | Отказ доступа без прав, поле «админ», первый юзер = админ, метрики 401/403/200 | admin-смоук **16 passed**, auth **2 passed**, метрики **3 passed** |
> | S8.7 | 17 типов уведомлений и их синхронизация фронт↔бэк | EVENT_MAP=17, метки=17, sync-тест **8 passed** |
> | S8.10 | Заголовки безопасности | **9 passed** |
> | S8.13 | Единый формат списков (PaginatedResponse/unwrapPaginated) | типы на месте, runtime-крашей нет |
> | S8.14 | Замеры скорости на 3 путях (сигнал/ордер/Telegram) | декораторы на месте, observability **11 passed** |
> | S8.16 | 9 контрактов фронт↔бэк | потребители на месте (Health, Sparkline, Telegram-тест, Sidebar) |
> | S8.17 | Тесты/типы/линтер/безопасность | backend **1890 passed**, frontend **631 passed**, tsc **0 ошибок**, eslint **0 предупреждений**, bandit **0 medium+** |
>
> ⚠️ Не входило в автопроверку (нужен живой прогон под нагрузкой / клики): целевые числа скорости из S8.14 (сейчас метрики на тестовых данных), playwright-сценарии, и все 👆-пункты ниже — они за тобой.

---

### 👆 S8.1 — Раздел «Администрирование» только для админа

**О чём:** в приложении есть служебный раздел для администратора. Обычный пользователь не должен ни видеть его в меню, ни заходить по прямой ссылке.

- [ ] **Меню админа.** Зайди под админом, посмотри левое меню.
  - ✅ есть пункт **«Администрирование»** (значок щита).
  - ❌ пункта нет.
- [ ] **Меню обычного.** Зайди под обычным пользователем, посмотри меню.
  - ✅ пункта «Администрирование» **нет**.
  - ❌ пункт виден (так быть не должно).
- [ ] **Прямая ссылка под обычным.** В адресной строке допиши `/admin`, нажми Enter.
  - ✅ выкидывает на главную, всплывает уведомление «Доступ ограничен».
  - ❌ страница админки открылась.
- [ ] **Страница админа.** Под админом открой «Администрирование».
  - ✅ заголовок «Администрирование», зелёный бейдж «admin», есть ссылка на метрики.
  - ❌ пустая или битая страница.

> 🤖 ✅ **Проверено (2026-06-30):** admin-смоук **16 passed** (доступ без прав — отказ), поле `is_admin` в профиле `/auth/me` есть, первый зарегистрированный юзер = админ, выдача прав через консоль на месте. Тебе остаются 👆 клики выше.
> 📝 Заметки/баги:

### 👆🤖 S8.2 — Страница метрик `/admin/metrics`

**О чём:** служебная страница с графиками-метриками системы (пока на тестовых данных).

- [ ] **Под админом.** Открой «Администрирование» → ссылку на метрики.
  - ✅ страница открылась, видно **4 графика** (задержки сигнал→ордер, скорость дашборда, скорость Telegram-команд, темп бэктестов).
  - ❌ страница не грузится / графиков нет.
- [ ] **Обновление.** Перезагрузи страницу (F5).
  - ✅ графики перерисовываются.
- [ ] **Тёмная тема.** Страница в целом тёмная (допустимо, что графики чуть светлее — это известное ограничение).

> ⚠️ Данные на графиках сейчас **тестовые (mock)** — это нормально для текущего этапа.
> 🤖 ✅ **Проверено (2026-06-30):** тест админ-метрик **3 passed** — без входа отказ (401), под обычным юзером отказ (403), под админом 200. Тебе остаются 👆 клики выше (4 графика, тёмная тема).
> 📝 Заметки/баги:

### 👆 S8.3 — Защита от «белого экрана» (одна поломка не валит всё)

**О чём:** если один блок на странице сломается, остальная страница должна остаться рабочей, а не превратиться в белый экран.

- [ ] **Главная страница (дашборд).** Просто открой её.
  - ✅ все блоки (Баланс, Здоровье системы, Позиции, мини-график) видны и работают.
- [ ] **Если какой-то блок сломан** — на его месте аккуратная плашка с предупреждением (треугольник) и кнопка «Повторить», а соседние блоки работают.
  - ✅ соседние блоки целы, кнопка «Повторить» пытается перезагрузить блок.
  - ❌ вся страница белая/пустая.
- [ ] **График.** Открой страницу графика — она тоже защищена такой плашкой при сбое.

> Это «страховка». В норме поломок нет, и проверять особо нечего — главное, что приложение не «падает целиком».
> 📝 Заметки/баги:

### 👆 S8.4 — Смена статуса стратегии прямо в списке

**О чём:** у каждой стратегии есть статус (Черновик, Протестирована, Paper Trading и т.д.). Менять его можно кликом по цветному бейджу в списке стратегий.

- [ ] **Где смотреть:** главная страница → список стратегий. У каждой строки — цветной бейдж статуса.
  - ✅ цвет соответствует статусу: Черновик — серый, Протестирована — синий, Paper Trading — жёлтый, Real Trading — красный, Пауза — оранжевый, Архив — серый.
- [ ] **Клик по бейджу** → раскрывается меню со всеми статусами; недоступные переходы — серые (неактивные).
- [ ] **Выбери новый статус.**
  - ✅ статус меняется сразу (не дожидаясь сервера), затем зелёное уведомление «Статус обновлён».
  - ❌ ничего не меняется или красная ошибка без отката.
- [ ] **Фильтр «Пауза».** Над списком есть переключатель фильтров — среди них есть «Пауза».

> 📝 Заметки/баги:

### 👆 S8.5 — Виджеты на главной странице (4 блока в ряд)

**О чём:** на дашборде в один ряд стоят 4 информационных блока: мини-график за 24ч, Здоровье системы, Баланс, Позиции.

- [ ] **Мини-график за 24 часа.** Есть блок с маленьким графиком цены выбранного тикера за сутки.
  - ✅ график рисуется; если данных нет — «Нет данных»; если тикер не выбран — «Выберите тикер».
- [ ] **Здоровье системы.** Блок показывает состояние: соединение с брокером, планировщик, защита от рисков.
  - ✅ цвет бейджа: зелёный — всё ок, жёлтый — предупреждение, красный — сработала защита.
- [ ] **Баланс.** Блок с балансом и мини-историей (график начинается с момента первой активности, а не с нуля времени).
- [ ] **Состояния блоков.** У всех блоков при загрузке — «скелетон» (серые плашки), при ошибке — кнопка «Повторить», при пустоте — «Нет данных».
- [ ] **Все 4 блока** стоят аккуратно в один ряд (на узком экране — перестраиваются в столбик).

> 📝 Заметки/баги:

### 👆 S8.6 — Кнопка «проверить Telegram» в мастере настройки

**О чём:** при первом запуске есть мастер настройки; на шаге уведомлений можно указать свой Telegram-бот и отправить тестовое сообщение.

- [ ] **Где:** мастер первого запуска → шаг 4 «Уведомления» → раскрывающийся блок «Свой бот».
- [ ] **Поля.** При раскрытии — поле для токена бота (скрытое, точками) и поле для chat_id.
- [ ] **Кнопка «Отправить тестовое сообщение».**
  - ✅ пока оба поля пустые — кнопка неактивна; после заполнения — активна.
- [ ] **Нажми кнопку** (с реальными токеном и chat_id).
  - ✅ «✅ Сообщение отправлено» и сообщение реально приходит в Telegram.
  - ❌ «❌ Ошибка: …» (тогда проверь токен/chat_id) или ничего не происходит.
- [ ] **Завершение мастера** включает уведомления для важных событий автоматически.

> 📝 Заметки/баги:

### 👆🤖 S8.7 — Список типов уведомлений (настройки)

**О чём:** на странице настроек уведомлений есть список всех типов событий с переключателями каналов (в приложении / Telegram / Email).

- [ ] **Где:** Настройки → Уведомления.
  - ✅ список содержит **17 типов** событий (сделки, ошибки, статус сессий, бэктест завершён, дивиденды и т.д.), у каждого понятное русское название.
- [ ] **По умолчанию** включён только канал «в приложении», остальные выключены.
- [ ] **Реальная доставка.** Включи Telegram + Email для типа «сделка исполнена» → соверши реальную сделку.
  - ✅ уведомление приходит во все 3 канала (в приложении, Telegram, Email).

> 🤖 ✅ **Проверено (2026-06-30):** на сервере 17 типов, в интерфейсе 17 меток, синхронны — sync-тест **8 passed**. Тебе остаётся 👆 проверить реальную доставку в 3 канала по живой сделке.
> 📝 Заметки/баги:

### 👆 S8.8 — Рисование на графике (линии, прямоугольники, подписи)

**О чём:** на графике можно рисовать инструменты разметки и затем их редактировать.

- [ ] **Где:** страница графика (например, тикер SBER), таймфрейм «День».
- [ ] **Нарисуй** трендовую линию, прямоугольник, подпись.
- [ ] **Перетаскивание.** Схвати фигуру мышью и подвигай.
  - ✅ двигается плавно, после перезагрузки страницы остаётся на месте.
- [ ] **Изменение формы.** Наведи на угол/конец фигуры — появляется маркер, тяни его.
  - ✅ форма меняется, курсор подсказывает (стрелка изменения размера).
- [ ] **Удаление.** Выдели фигуру → нажми Backspace или Delete. Либо правый клик → «Удалить».
  - ✅ фигура удаляется.
- [ ] **Esc** снимает выделение и возвращает обычный курсор.
- [ ] **Внутридневные таймфреймы.** Переключись на 5м/15м/1ч и нарисуй новую линию.
  - ✅ новые фигуры рисуются и стоят правильно.

> ⚠️ Известное ограничение: очень старые фигуры могут не отображаться на внутридневных ТФ (это намеренно, чинится позже). Новые — работают.
> 📝 Заметки/баги:

### 👆 S8.9 — AI-описание стратегии → блоки

**О чём:** в редакторе стратегий можно описать стратегию текстом и превратить в блоки (в текущей версии — на упрощённых/тестовых данных).

- [ ] **Где:** редактор стратегии (Blockly) → кнопка «AI-описание».
  - ✅ открывается панель с полем для текстового описания.
- [ ] **Введи описание** и нажми кнопку «Применить к блокам».
  - ✅ в редакторе появляются блоки стратегии.
- [ ] **Предупреждения.** Если что-то не распозналось — плашка с предупреждением (например, «не найден тикер X»).
- [ ] **Во время обработки** кнопка заблокирована, крутится индикатор загрузки.

> 📝 Заметки/баги:

### 🤖 S8.10 — Заголовки безопасности (проверяю я)

**О чём:** ответы сервера должны содержать защитные заголовки (защита от встраивания в чужие сайты, ограничение источников и т.п.). Глазами это не видно — проверяется инструментами.

- [x] 🤖 Все ответы сервера содержат набор заголовков безопасности (защита от iframe, ограничение источников скриптов и др.).
- [x] 🤖 Приложение нельзя встроить в чужой сайт через iframe.

> 🤖 ✅ **Проверено (2026-06-30):** тест заголовков безопасности **9 passed** (HSTS, X-Frame-Options: DENY, X-Content-Type-Options, CSP, Referrer-Policy, Permissions-Policy).
> ⚠️ Известное: 2 авто-теста сети иногда «мигают» (нестабильны) — это уже в списке на доработку, не баг приёмки.
> 📝 Заметки/баги:

### 👆 S8.11 — Панель фоновых бэктестов

**О чём:** бэктесты можно запускать «в фоне» и следить за ними через значок в шапке.

- [ ] **Значок в шапке** показывает число активных + завершённых фоновых бэктестов.
- [ ] **Клик по значку** → всплывающий список бэктестов с прогрессом и кнопками «Открыть результат».
- [ ] **Тепловая карта (grid).** Для завершённого grid-бэктеста есть кнопка, открывающая окно с тепловой картой результатов.
- [ ] **Автосворачивание.** Когда активных не осталось, а завершённые есть — панель сама сворачивается через ~5 секунд.
- [ ] **Лимит.** Больше 3 фоновых бэктестов одновременно запустить нельзя (4-й даёт уведомление о лимите).

> 📝 Заметки/баги:

### 👆 S8.12 — Вкладки результатов бэктеста

**О чём:** страница результата бэктеста разбита на вкладки, есть интерактив по сделкам.

- [ ] **Где:** открой любой завершённый бэктест.
  - ✅ есть 4 вкладки: **Обзор / График / Показатели / Сделки**.
- [ ] **Зоны сделок на графике.** На ценовом графике подсвечены диапазоны сделок; при наведении зона выделяется.
- [ ] **Клик по сделке.** Во вкладке «Сделки» кликни по строке сделки.
  - ✅ раскрывается панель с деталями: время и цена входа/выхода, причина, прибыль/убыток, длительность.
- [ ] **Вкладка «Обзор»** содержит гистограмму прибыли/убытка и «бублик» прибыльные/убыточные.
- [ ] **Кнопка «Запустить торговлю из бэктеста»** открывает окно запуска с уже заполненными параметрами (тикер, таймфрейм, капитал).

> 📝 Заметки/баги:

### 🤖 S8.13 — Единый формат списков от сервера (проверяю я)

**О чём:** технический пункт — приложение устойчиво обрабатывает списки данных от сервера и не «падает», если формат необычный.

- [x] 🤖 Списки (позиции, история и т.п.) грузятся без сбоев; при загрузке — индикатор, при пустоте — «Нет данных».

> 🤖 ✅ **Проверено (2026-06-30):** единый тип `PaginatedResponse` + защитный `unwrapPaginated()`/`isPaginatedResponse()` на месте; вся сборка типов проходит (tsc 0 ошибок), runtime-крашей нет.
> Видимая часть для тебя 👆: открой страницы с таблицами (Позиции, Бэктесты) — не должно быть ошибок, на пустых списках «Нет данных».
> 📝 Заметки/баги:

### 🤖 S8.14 — Скорость и метрики (проверяю я)

**О чём:** замеры скорости ключевых операций и целевые показатели. Проверяется прогоном/замерами, не кликами.

- [x] 🤖 Ключевые операции (обработка сигнала, выставление ордера, обработка Telegram-команды) замеряются по времени.
- [ ] 🤖 Целевые показатели: сигнал→ордер быстрее 0,5с; Telegram-команда быстрее 3с; загрузка дашборда быстрее 2с. — **требует живого замера под нагрузкой** (метрики сейчас на тестовых данных), автоматически не подтверждается.

> 🤖 ✅ **Проверено (2026-06-30):** замеры (`@timed_event`) стоят на всех 3 путях (`signal.process`, `order.place`, `telegram.handle`), observability-тест **11 passed**. Конкретные числа p95 — только живым прогоном (см. ⚠️ выше).
> Видимая часть для тебя 👆: дашборд открывается быстро, без заметных подвисаний.
> 📝 Заметки/баги:

### ✅ S8.15 — 6 сквозных сценариев

Полностью покрыто **Шагом 2** выше (6 сценариев «от регистрации до закрытия позиции»). Отдельно проходить не нужно — поставь крестик, когда закончишь Шаг 2.

- [ ] Покрыто Шагом 2.

> 📝 Заметки/баги:

### 🤖 S8.16 — Сверка договорённостей между частями (проверяю я)

**О чём:** технический пункт — что блоки фронтенда правильно потребляют данные бэкенда (9 «контрактов»). Проверено анализом кода.

- [x] 🤖 Все 9 контрактов выполнены (расширенное «Здоровье», мини-график, история баланса, тест Telegram, формат списков, признак админа, страница метрик, синхронизация типов уведомлений).

> 🤖 ✅ **Проверено (2026-06-30):** фронт-потребители на месте — `HealthWidget` (cb_state), `MiniSparkline`/`getSparkline`, кнопка теста Telegram в мастере, условный пункт админа в Sidebar; плюс серверные контракты подтверждены тестами S8.1/S8.2/S8.7/S8.10 выше.
> 📝 Заметки/баги:

### 🤖 S8.17 — Итоговые технические проверки (проверяю я)

**О чём:** прогоны тестов, проверка типов, линтер, безопасность, контраст тёмной темы. Это запуски команд, не клики.

- [x] 🤖 Авто-тесты фронтенда проходят. — **631 passed**.
- [x] 🤖 Проверка типов — без ошибок. — `tsc --noEmit` **0 ошибок**.
- [x] 🤖 Линтер — без ошибок. — eslint **0 ошибок, 0 предупреждений** (строже baseline «9 warnings»).
- [x] 🤖 Авто-тесты бэкенда проходят, покрытие ≥ 80%. — **1890 passed, 1 xfailed**, покрытие **85%**.
- [x] 🤖 Сканер безопасности — без серьёзных находок. — **bandit: 0 medium+ по коду**. safety: 5 известных CVE зависимостей (1 ignored) — ранее вынесены в S9-backlog (см. `security_audit_s8.md`), бамп версий тут не делал (вне рамок приёмки).
- [ ] 👆 **Контраст тёмной темы.** Глазами: новые блоки (мини-график, Здоровье, Баланс) — текст и цвета (зелёный/красный/жёлтый) читаются, не сливаются с фоном.
- [ ] 👆 **Свёрнутое меню.** Сверни левое меню — при наведении на иконки всплывают подсказки с названиями.

> 🤖 ✅ **Проверено (2026-06-30):** все технические пункты выше — зелёные (цифры в строках). Числа выше baseline S8 (тесты добавлялись по ходу багфиксов) — это норма.
> Тебе остаются 2 👆-пункта: контраст тёмной темы и подсказки в свёрнутом меню.
> 📝 Заметки/баги:

---

## Найденные баги

> Формат: `BUG-N | severity: lethal/critical/medium/low | где | что | приоритет фикса`

- **BUG-28 ✅ FIXED 2026-06-24 (S8R-ACCEPTANCE-TRADE-MARKERS-OUT-OF-WINDOW) | severity: MEDIUM (косметика графика — маркеры входа/выхода старых сделок наваливаются кучей на левый край при прокрутке назад; данные/торговля корректны) | `frontend/src/components/charts/sequentialIndex.ts` + `CandlestickChart.tsx` | testuser1 (скриншот 2026-06-24): на графике сессии #6 при прокрутке влево (назад по истории) маркеры Buy/Sell со ценниками искажались — несколько меток наваливались друг на друга у левого края (наблюдалось для сделок 11.06 @322/321.58 при просмотре ~20–24.06).** Диагностика (systematic-debugging): график intraday в **sequential-index mode** (ось X = индекс бара, не unix). Маркеры рисует `TradeMarkerPrimitive` по `(time=index, price)`, где index вычислял `tradeTimeValue` как **ближайшую** свечу к времени сделки (`bestIdx` = argmin|indexToUnix−mskSec|). Если сделка **вне загруженного окна** (старее самой ранней подгруженной свечи), ближайшая = index 0 → ВСЕ такие старые сделки клампились на index 0 и наваливались кучей на левый край. Фильтр `0 ≤ idx < mapLen` это не ловил (idx=0 валиден). Подтверждено по БД: сделки #6 — #24/#26 (322/321.58) от **11.06**, #31 (311.54) от 19.06, #39 (302.07) от 22.06; при окне ~20.06+ метки 11.06 валились слева. (В default-режиме D/W/M баг не проявляется — там реальный unix → `timeToCoordinate` сам отдаёт null для вне-диапазона, primitive пропускает.) **ФИКС (2026-06-24, TDD):** новый чистый хелпер `nearestIndexInWindow(indexToUnix, targetSec)` в `sequentialIndex.ts` — возвращает ближайший индекс, но `null`, если ближайшая свеча дальше ~2 интервалов баров (интервал = min положительная разница соседних → овернайт-гэпы не раздувают tolerance). `tradeTimeValue` → тип `Time | null`, sequential-ветка использует хелпер; `tradeToMarkers` пропускает маркеры с `time===null`. Итог: старые сделки вне окна СКРЫВАЮТСЯ и появляются на правильных свечах только когда их история подгружена. **Тесты (RED→GREEN):** `__tests__/sequentialIndex.test.ts` +8 кейсов (пусто/точное/в окне/далеко до/далеко после/край в tolerance/край за tolerance/овернайт-гэп). charts-сюита **103 passed**, `tsc --noEmit` 0, `eslint --max-warnings 0` 0. График грузится с 0 console errors. Визуальную прокрутку-назад заказчик подтверждает воспроизведением (юнит-замок точного сценария на месте). НЕ путать с gotcha-24 (там drawing-примитивы, здесь — trade-маркеры; тот же sequential-mode класс).
- **BUG-27 ✅ FIXED 2026-06-24 (S8R-ACCEPTANCE-STREAM-NO-SELFHEAL) | severity: HIGH (live-блокер — активная сессия числится «Активна», но её T-Invest-стрим мёртв и не самовосстанавливается → 0 live-свечей, сессия не торгует, без алерта) | `backend/app/trading/runtime.py` (нет runtime-надзора за стримом) + `backend/app/broker/tinvest/multiplexer.py` (watchdog живёт ВНУТРИ задачи) | Заказчик заметил расхождение: карточка «Состояние систем» → T-Invest **OFFLINE** (красный), при этом футер «Брокер: подключён» (зелёный) и видны актуальные цены SBER.** Диагностика (systematic-debugging, 2026-06-24): три индикатора меряют **разные слои** — карточка `/health.tinvest_connected = any(m.connected for m in multiplexer._singletons)` (живой gRPC-стрим), футер `accounts.some(is_active)` (настроен ли счёт), цены — `MarketDataService.get_candles` → REST+`OHLCVCache` (по запросу, `_save_to_cache` только из REST-пути [service.py:131], стрим в кэш не пишет). Поэтому зелёный футер + свежие цены НЕ противоречат мёртвому стриму. **Корень:** в долгоживущем воркере (supervisor PID 46019 с 3 июня) `/health` отдавал `tinvest_connected=false` при **4 активных торговых сессиях** (#1 paper SBER 1h, #2 paper LKOH 5m, #3 sandbox SBER 5m, #6 sandbox SBER 1m) — т.е. ни один `TInvestStreamMultiplexer` не был подключён, хотя reconnect-цикл `_run_stream` спроектирован «никогда не сдаваться» (бесконечный `while`, backoff cap 30с). Значит сама задача `_run_stream` исчезла/умерла (вероятный триггер — гонка при `uvicorn --reload` или отмена задачи), а **на уровне runtime/сессии её перезапускать некому**: watchdog BUG-25 (idle-timeout→reconnect) работает только ВНУТРИ живой `_run_stream`. Сессии оставались `status='active'` без свечей (последние `signal.process` отсутствовали). **ПРОВЕРКА перезапуском (заказчик дал добро):** корректно остановил uvicorn (SIGTERM→SIGKILL supervisor+worker) и поднял заново с файловым логом (без песочницы — чтобы исходящий gRPC к T-Invest не блокировался). Результат: `restore_complete restored=4 skipped=0 mismatched=0`; `stream_started` для всех 4 каналов (market:LKOH:5m, market:SBER:1h, market:SBER:5m, market:SBER:1m); `multiplexer_connected` ×2 (два токена: user1 + user4-sandbox); `/health.tinvest_connected=true` (стабильно, без разрывов после старта); пошли `signal.process` + `max_positions_reached` по сессиям 1/2/3/6 — **live-свечи реально потекли**. 0 ошибок/трейсбеков в логе. Вывод: стримы доступны, проблема была именно в отсутствии самовосстановления в старом поколении воркера. **Точный триггер смерти задачи не установлен** — dev-логи шли в stdout (файла не было) и потеряны. **ПРЕДЛАГАЕМЫЙ ФИКС (в рамках приёмки, критический путь → TDD + code-review):** (1) runtime-watchdog: периодическая проверка, что у каждой `active`-сессии есть живой стрим (`_StreamEntry` существует + `multiplexer.connected` + свежий `last_event_at` в торговые часы); иначе — пере-подписка + публикация `connection.lost`; (2) отразить здоровье стрима в статусе сессии/карточке, чтобы «Активна» не вводила в заблуждение при мёртвом канале; (3) включить файловое логирование dev-бэкенда (чтобы поймать триггер при рецидиве). НЕ путать с BUG-25 (там фикс idle-таймаута ВНУТРИ `_run_stream`; здесь — отсутствие надзора НАД задачей). **ФИКС (2026-06-24, TDD):** (1) `stream_manager.is_stream_healthy(ticker, tf)` + `ensure_stream(...)`: health-сигнал — **живость фоновой задачи** мультиплексера (`_stream_task` не `done()`), сознательно НЕ `connected` (тот мигает False при штатном reconnect → watchdog бы дрался с backoff мультиплексера, thrash; см. **gotcha-34**). Нездоров = нет `_StreamEntry` (subscribe упал при старте) ИЛИ задача `done()` → `ensure_stream` сбрасывает протухший entry и пере-подписывается (`multiplexer.subscribe → start()` пересоздаёт задачу). (2) `runtime._recover_stale_streams()` встроен в существующий `_periodic_recovery_loop` (60с): для каждой active-сессии проверяет здоровье и форсит пере-подписку; guard на гонку с concurrent stop (`session.status != 'active'` + `session_id not in self._listeners`). (3) Файловые логи dev-бэкенда: `scripts/start.sh` → `tee logs/dev.log` (+ `.gitignore`); SQL-эхо развязано от DEBUG — новый флаг `SQL_ECHO` (default False, `echo=settings.SQL_ECHO`), иначе DEBUG=true топил лог в 71k SQL-строк. **Доводка читаемости лога (2026-06-24):** после выключения SQL-эха всплыл второй источник шума — per-candle INFO `timed_event` (signal.process) + `max_positions_reached` (~98% строк, 22327 всего). structlog по умолчанию НЕ фильтрует уровень (debug печатался наравне с info), поэтому: (а) добавлен `LOG_LEVEL` (default INFO) + `app/common/logging_config.py::configure_logging()` (`make_filtering_bound_logger`), вызывается в `main.py` до первого лог-вызова; (б) оба per-candle события опущены `info → debug` ([engine.py](../../Develop/backend/app/trading/engine.py), [observability.py](../../Develop/backend/app/common/observability.py)); фикстура `test_observability.captured_logs` укреплена (ловит debug независимо от глобального уровня). Метрики `/admin/metrics` сейчас на mock-данных, эти логи не читают — потерь нет. Результат: лог в работающем приложении **62 строки вместо 22327**, flood=0, полезные события целы; debug-трассировка по требованию — `LOG_LEVEL=DEBUG` в .env. **Доводка детерминизма тестов (2026-06-24):** при полном прогоне всплыли 32 локальных фейла delivery-тестов (`test_event_delivery_e2e`, `test_notification_service::test_dispatch_external_returns_channels`, `unit/test_notification/test_dispatch_all_events`) — НЕ от логирования, а pre-existing: локальный `.env` имеет `DEV_MODE=true` (фикс Telegram-спама), `dispatch_external` при DEV_MODE → `[]`, а тесты доставки не пинили DEV_MODE (в CI `DEV_MODE=false` → зелено). Фикс: autouse-фикстура `_force_dev_mode_off` (`monkeypatch DEV_MODE=False`) в `tests/test_notification/conftest.py` + новый `tests/unit/test_notification/conftest.py`; suppression-тест сам ставит True в теле (побеждает). После фикса: 4 сюиты **1450 passed, 1 xfailed, 0 failed**. **Тесты (RED→GREEN):** `tests/test_market_data/test_stream_health.py` (10 — health для no-entry/alive/dead/reconnecting/DWM + ensure_stream noop/missing/dead) + `tests/test_trading/test_runtime_recovery.py::TestStreamWatchdog` (2 — пере-подписка нездорового / пропуск здорового). Регресс trading+market_data **228 passed**. **Code-review (high-effort, до коммита):** поймал критичный дефект первой версии — health на `connected` дал бы thrash/драку с reconnect'ом; исправлено на task-aliveness + добавлен stop-race guard. Заведён **gotcha-34**. **Проверка перезапуском:** новый код поднят (`backend/logs/dev.log`), `tinvest_connected=true`, restore_complete restored=4, лог чистый (202 строки vs 71625, SQL-эхо off). **Ограничение (известное, документировано):** per-figi потеря подписки при живом сокете не детектируется (частично — idle-watchdog BUG-25); полное решение через `last_event_at`+торговые часы — кандидат на развитие. Точный триггер исходной смерти задачи не установлен (старые stdout-логи потеряны) — файл-логи добавлены для поимки рецидива (проверить ~2026-06-26). **CI-доводка (2026-06-25):** после пуша коммита BUG-26/27/28 GitHub Actions упал на шаге Lint в двух job'ах. (1) backend `ruff` — `E402` ×N: вызов `configure_logging()` стоял на уровне модуля **до** импортов моделей/роутеров в `main.py` (statement до import = «import not at top of file»). Фикс: вызов перенесён вниз, сразу после блока импортов, перед `_main_logger`/`app = FastAPI(...)` — по-прежнему до первого реального лог-вызова (lifespan), ruff чист. Локально пропущено, т.к. гоняли `tsc/eslint/pytest`, но не `ruff`. (2) frontend `eslint --max-warnings 0` — `react-refresh/only-export-components` в `LaunchSessionModal.tsx:48` (`export function parseParityError`): **pre-existing** (введён коммитом `c99f31f` BUG-23 UI, не BUG-26/27/28; CI был красным с 2026-06-11) — компонент-файл экспортировал и компонент, и функцию, ломая Fast Refresh. Фикс по сообщению самого правила: `parseParityError` + тип `ParityBlock` вынесены в новый модуль `frontend/src/components/trading/parseParityError.ts`; импорт в `LaunchSessionModal.tsx` и в `__tests__/parseParityError.test.ts` обновлён (чистый перенос, поведение неизменно). **Gates (локально, как в CI):** backend `ruff check .` чист, `mypy app/main.py` чист, `py_compile`+import OK; frontend `eslint .` 0, `tsc --noEmit` 0, `vitest run` **89 файлов / 626 тестов passed**.
- **BUG-26 ✅ FIXED 2026-06-24 (S8R-ACCEPTANCE-GHOST-ROW) | severity: LOW (косметика — пустая строка-призрак во вкладке «Сделки», БД и торговая логика корректны, лечится refresh) | `frontend/src/components/trading/SessionDashboard.tsx` + `frontend/src/pages/ChartPage.tsx` | testuser1 (E2E 2026-06-11): на странице сессии #6 после ручного закрытия позиции и мгновенного авто-переоткрытия во вкладке «Сделки» появилась строка с пустыми полями (дата «—», «Лоты: /», цены/PnL «—»). Вопрос заказчика: «связано ли это с двумя открытыми сессиями SBER 1m?»** Диагностика (systematic-debugging, 2026-06-24): **НЕТ, не связано** — обе сделки (#24 closed 322→321,58 pnl −13,02; #26 filled 321,58) принадлежат `session_id=6`, кросс-сессионного перетекания нет; сессии #5/#6 независимы (хотя дублируют исполнение одной версии стратегии sv=92 — отдельное наблюдение, не баг). Корень призрака: backend публикует в канал `trades:{session_id}` **лёгкое** WS-событие `trade.filled`/`trade.closed` с минимальным payload `{type, session_id, trade_id, ticker, direction, price}` ([runtime.py:1358-1388](../../Develop/backend/app/trading/runtime.py#L1358)) — ключ называется `trade_id` (НЕ `id`), полей `opened_at`/`*_lots`/`entry_price`/`pnl` нет. Фронт ([SessionDashboard.tsx:73] до фикса) кастил этот payload `as unknown as LiveTrade` и клал в стор через `addTradeFromWS`, чья идемпотентность (фикс W8f BUG-5) ищет существующую сделку по `trade.id` — а `id` в payload `undefined` → dedup всегда мимо → частичный объект добавлялся новой строкой; `TradesTable` рисовал `direction='buy'`→«▲ Long», остальное `undefined`→«—»/«/». Тот же `addTradeFromWS` вызывался и в `ChartPage` (где таблицы сделок нет вовсе) — загрязнял общий `store.trades`. **Проверка**: REST-перезагрузка вкладки давала 2 чистые строки (призрак исчезал) — подтверждение, что дефект чисто клиентский, БД верна. **ФИКС (2026-06-24):** в `SessionDashboard.wsCallback` на `trade.filled`/`trade.closed` вместо `addTradeFromWS(partial)` вызываем `fetchTrades(sessionId)` — перечитываем авторитетный список из REST (как уже делалось для позиций рядом). В `ChartPage` вызов `addTradeFromWS` убран полностью (страница не показывает сделки; достаточно `fetchPositions`). Неиспользуемые импорты `LiveTrade` и селекторы `addTradeFromWS` удалены из обоих компонентов; store-метод `addTradeFromWS` оставлен (публичный API стора, покрыт `tradingStore.test.ts`). **Gates**: `tsc --noEmit` 0 errors; `eslint --max-warnings 0` 0 errors; `vitest tradingStore.test.ts` 8/8 passed; Vite-бандл собирается чисто (нет build-overlay). **Замечание (не фикшено, на решение заказчика)**: ветка фикса (`trade.filled/closed`→`fetchTrades`) рендерится только на живом fill-событии — визуальное E2E-подтверждение требует реального закрытия позиции в sandbox; статический рендер таблицы из REST не изменён. **Bonus-наблюдение (low, не в scope)**: время сделок в таблице рисуется в TZ браузера (UTC+2 на тест-машине), а футер форсит МСК (UTC+3) — рассинхрон отображения времени; на данные не влияет.
- **BUG-25 ✅ FIXED 2026-06-11 (S8R-ACCEPTANCE-SP-B) | severity: HIGH (блокер live — тихо встающий свечной поток → 0 сделок при статусе сессии «Активна», без алерта) | `backend/app/broker/tinvest/multiplexer.py` (+ `app/broker/tinvest/stream_manager.py`) | Исходная находка #3 (упомянута в BUG-22 как «1m-свечи прекратились 2026-06-04 10:19»): sandbox-сессия #5 (SBER 1m) открыла 1 позицию в 13:47 и встала.** Диагностика (systematic-debugging, фоновый агент 2026-06-11): в gRPC `market_data_stream` **нет обработки Ping/keepalive**. `_run_stream` крутит `async for response in client.market_data_stream(...)` ([multiplexer.py:241,261](../../Develop/backend/app/broker/tinvest/multiplexer.py#L241)); `_request_iterator` шлёт только SUBSCRIBE/UNSUBSCRIBE, никогда `ping`/`ping_settings`; ответы `response.ping` молча игнорируются; канал создаётся без `grpc.keepalive_time_ms`. Reconnect ([multiplexer.py:266]) срабатывает **только на Exception**. При «тихом» обрыве (полуоткрытый TCP / RST не дошёл) `async for` блокируется навсегда без исключения → reconnect не вызывается → канал `market:<ticker>:<tf>` пустой навсегда → `_SessionListener`/`SignalProcessor` пассивно ждут → 0 свечей → 0 сигналов; сессия числится `active`, `connection.lost` не публикуется. **Runtime-подтверждение (E2E 2026-06-11)**: новая sandbox #6 (SBER 1m) стартовала и СРАЗУ исполнила `buy` → поток СТАРТУЕТ корректно (warmup + live-свечи приходят); значит симптом — «встаёт спустя время», а не «не стартует». Долгий простой (часы) в окне playwright не воспроизводится — root cause доказан статически. **Bonus-находка (отдельно, low)**: enum-mismatch — `multiplexer` оборачивает значение из `CandleInterval`-таблицы в `SubscriptionInterval(...)` ([multiplexer.py:326,346]); для 1m/5m значения совпадают, но для 2m/3m/10m/30m/2h/4h — расходятся (неверная подписка). **Предлагаемый фикс**: (1) `ping_settings` + активная отправка `MarketDataRequest(ping=...)` по таймеру + обработка `response.ping`; (2) gRPC keepalive options в `AsyncClient`/`create_channel` (`keepalive_time_ms=30000`, `keepalive_timeout_ms=10000`, `keepalive_permit_without_calls=1`) → тихий обрыв даст Exception → существующий reconnect сработает; (3) watchdog на `_StreamEntry.last_event_at` ([stream_manager.py:104]): нет событий > N×timeframe в торговые часы → форс-reconnect; (4) починить enum-mismatch. **ФИКС (2026-06-11, TDD):** в `multiplexer._run_stream` стрим теперь драйвится вручную `await asyncio.wait_for(stream_iter.__anext__(), timeout=self._stream_idle_timeout)` (idle=180с > серверного ping ~120с) — при тихом обрыве TimeoutError → `raise ConnectionError` → существующая ветка backoff+resubscribe пересоздаёт стрим. Плюс gRPC keepalive (`AsyncClient(options=[keepalive_time_ms=60000, keepalive_timeout_ms=20000, keepalive_permit_without_calls=1])`) как транспортная защита. `finally: stream.aclose()`. **Enum-mismatch (bonus) — НЕ воспроизводится** в текущем SDK (0.2.0-beta117): значения `CandleInterval` и `SubscriptionInterval` совпадают для всех используемых ТФ (1m..4h/W/M; расходятся лишь суб-минутные 5s/10s/30s, которых в подписках нет) — проверено `enum`-инспекцией, правка не нужна. **Тест (RED→GREEN):** `test_tinvest_multiplexer.py::test_idle_stream_triggers_reconnect` (тихий стрим → reconnect; до фикса висел вечно, call_count=1). Регресс multiplexer 13/13. Код-ревью: APPROVED. **Watchdog-уровень (multiplexer, а не stream_manager) выбран как минимальный и полностью тестируемый.** НЕ в scope BUG-23/22.

- **BUG-24 ✅ FIXED 2026-06-11 (S8R-ACCEPTANCE-BUG-24) | severity: MEDIUM (любая Stochastic-стратегия падает в бэктесте; обходится — остальные 7 индикаторов работают) | `backend/app/strategy/code_generator.py:220-227` | Найдено при BUG-23 (parity-турнир, xfail Stochastic), подтверждено 2026-06-11.** `code_generator` генерит `bt.indicators.Stochastic(self.data, period=..., d_period=...)`, но у backtrader параметра `d_period` НЕТ — реальные параметры `period`, `period_dfast`, `period_dslow` (проверено `bt.indicators.Stochastic.params._getpairs()`). Любой бэктест Stochastic-стратегии падает с `TypeError: __init__() got an unexpected keyword argument 'd_period'`. **Связанное**: интерпретатор использует StochasticFast vs codegen Stochastic(slow) — задокументированный `xfail(strict=True)` в `tests/unit/test_strategy/test_signal_parity.py`; при фиксе кодогена выровнять fast/slow и снять xfail. **ФИКС (2026-06-11, TDD):** `code_generator.py:220` `d_period=` → `period_dfast=`. **Тест (RED→GREEN):** `test_signal_parity.py::test_stochastic_generated_code_runs_in_backtrader` — реальный прогон Stochastic-стратегии через `BacktestEngine._compile_strategy` + `Cerebro.run()` (НЕ `compile_restricted`, т.к. ошибка runtime, а не синтаксис): до фикса TypeError на инстанцировании, после — список сделок. Семантический xfail `test_signal_parity_stochastic_diverges` остался strict-xfail (StochasticFast vs slow — отдельно, паритет сигналов недостижим без смены семантики). Код-ревью: APPROVED.

- **BUG-23 ✅ FIXED 2026-06-10 (S8R-ACCEPTANCE-FIX-BUG-23) | severity: HIGH (расхождение backtest≠live: часть грамматики стратегий — пользовательский exit `condition_in_zone`, crossover, logic, MACD/BB/ATR/Stochastic — не работала в live, молча) | `backend/app/strategy/{indicators,ir,evaluator}.py` (новые) + `backend/app/backtest/parity.py` + live/backtest/гейт + frontend | Продолжение BUG-22 (находка #1): live `_blocks_to_sandbox` поддерживал только `condition_compare`>число и SMA/EMA/RSI. Корень — ДВА независимых переводчика блоков, которые расходились: бэктест → backtrader `code_generator` (полная грамматика); live → урезанный `_blocks_to_sandbox`. Стратегия в live вела себя не так, как в протестированном бэктесте (для sv=92 exit на `condition_in_zone` никогда не срабатывал).** Решение (спека+план: `docs/superpowers/specs|plans/2026-06-10-strategy-interpreter*`): **единый интерпретатор** `evaluate(ir, candles)` для live + **differential-сверка** в бэктесте (один движок логики, расхождение доказывается на каждом бэктесте). Реализовано — 17 задач по TDD (subagent-driven, два-стадийное ревью), 15 коммитов `a0a825c`…`50119f1` на ветке `s8r/bug-23-interpreter`: (1) `indicators.py` — 8 индикаторов, выверены к backtrader (RSI Wilder/SMMA, Bollinger population-std, ATR Wilder), parity-тесты vs `bt.indicators` rel=1e-9/1e-6; (2) `ir.py` `parse_blocks` — нормализация Blockly-формата → `StrategyIR`; (3) `evaluator.py` `evaluate`/`evaluate_series` (compare/in_zone/crossover/logic and/or/not; indicator-vs-число и -vs-индикатор; кеш серий O(N)); (4) live `SignalProcessor` → `evaluate` (`_blocks_to_sandbox` оставлен как legacy-fallback для IR-less; `generated_code` сохранён — оракул/показ/правка-параметров/SL-TP-fallback); (5) бэктест: shadow-прогон интерпретатора + сверка сигналов (критично) и P&L (информационно, «ориентировочно») → parity-поля в `backtests` (+Alembic `b8c4d2e6f3a1`), API; (6) **гейт live**: старт sandbox/real блокируется без parity-checked бэктеста (`parity_no_backtest`) и при расхождении сигналов (`parity_signals_diverged`, HTTP 409 + override + audit `parity_override`); **paper exempt**; (7) UI: бейдж сверки в результатах бэктеста + предупреждение/override-чекбокс в `LaunchSessionModal`; (8) правка параметров мигрирована на IR (`params.py`, регенерация `generated_code` из блоков). **Доказательство backtest=live**: `tests/unit/test_strategy/test_signal_parity.py` — турнир 7 форм (SMA-compare, EMA-in_zone, SMA-crossover, RSI-compare, logic_and, MACD, Bollinger) через РЕАЛЬНЫЙ backtrader (production compile + Cerebro) ⇄ интерпретатор: **7/7 signals_match**, по 2 реальные сделки на сторону (non-vacuous), fill_offset=1. **Известное расхождение**: Stochastic — интерпретатор StochasticFast, codegen Stochastic(slow) → `@pytest.mark.xfail(strict=True)`, задокументировано. **Новый найденный баг (НЕ в scope BUG-23, кандидат на отдельную задачу)**: `code_generator` генерит `bt.indicators.Stochastic(d_period=...)`, а параметр backtrader — `period_dfast` → любая Stochastic-стратегия **падает в бэктесте** (`TypeError: unexpected keyword 'd_period'`). **Тесты**: strategy-сюита **273 passed + 1 xfailed**, pyright 0 errors (per-file, venv), frontend vitest **618 passed** (parity 20) + `tsc -p tsconfig.app.json` чист по затронутым файлам; полный backend pytest — **1801 passed, 1 xfailed, 0 failed** (baseline 1656 + 145; xfail = Stochastic). **Поведенческое изменение**: старт sandbox/real-сессии теперь требует бэктест со сверкой движков (жёсткий блок) и блокирует при расхождении сигналов (с явным override). **Приёмочные доводки (2026-06-11, по находкам финального код-ревью):** (1) гейт `_check_parity_gate`: `parity_signals_match is False` → `is not True` — аномальный `parity_checked=True`+`match=None` больше не обходит гейт молча (+тест `test_sandbox_start_blocked_when_signals_match_is_none`); (2) зафиксирована намеренная версионность гейта (parity = свойство версии стратегии, не тикера) комментарием + тестом-замком `test_parity_is_version_level_not_ticker_specific`. **Полный backend pytest после доводок + BUG-24/25: 1806 passed, 1 xfailed, 0 failed.**

- **BUG-22 ✅ FIXED 2026-06-05 (S8R-ACCEPTANCE-FIX-BUG-22) | severity: HIGH (блокер Сценария 2 Live-торговли — любая блочная стратегия не открывает сделку) | `backend/app/trading/engine.py` `_blocks_to_sandbox` | testuser1 запустил sandbox-сессию #5 (SBER 1m, strategy version 92 «Первая стратегия»), за ~сутки 0 сделок: `last_signal_at`=NULL, `live_trades` пусто. Вопрос заказчика: «условия стратегии действительно не довели до сделки?» — НЕТ, условия ни при чём, это баг кодогенерации.** Корневая причина: live/sandbox-движок не исполняет backtrader `generated_code` (`class GeneratedStrategy`), а конвертирует `blocks_json` → sandbox-скрипт через `_blocks_to_sandbox` ([engine.py:478-486](../../Develop/backend/app/trading/engine.py#L478-L486)). Имена переменных индикаторов строились как `f"sma_{block_id}"` из сырого Blockly-id с символами, недопустимыми в Python-идентификаторах (`= % ^ ) [ | : ` ( `; реальный id sv=92: `7LDZ=b6%^r%y)4oI[6|u`). Код не компилировался → `CodeSandbox` success=False (`SyntaxError: unmatched ')'`) → `_execute_strategy`=None → `process_candle`=None → runtime молча без сигнала на КАЖДОЙ свече ([runtime.py:1252](../../Develop/backend/app/trading/runtime.py#L1252)). Воспроизведено на реальной sv=92: условие входа SMA(20)≈322 > 70 = True (должен быть `buy`); движок ДО фикса давал пустой output, ПОСЛЕ — `buy`. Бэктест не затронут (идёт по backtrader-пути). **Фикс**: имена переменных из монотонного индекса (`ind_sma_0`, `ind_ema_1`, …), block_id остаётся лишь ключом словаря `ind_vars`. **Тесты (TDD Red-Green)**: новый `tests/test_trading/test_engine_blocks_to_sandbox.py` — 6 тестов (compile × 4 hostile id, var-reference consistency, e2e SMA>70→buy через реальный CodeSandbox); RED `SyntaxError: unmatched ')'` → GREEN 6/6; регрессия `test_trading` **179 passed**, полный backend pytest **1656 passed, 0 failed**. **Сопутствующие находки (НЕ в scope, на решение заказчика)**: (1) exit-условие `condition_in_zone` не поддержано в `_blocks_to_sandbox` → `exit_sig` всегда False ([engine.py:635-639](../../Develop/backend/app/trading/engine.py#L635-L639)) — кандидат BUG-23; (2) SL/TP из management-блоков (3%/6%) игнорируются в sandbox-пути; (3) 1m-свечи прекратились на `2026-06-04 10:19` (поток market-data / рыночные часы — на verdict не влияет). **UI-верификация**: после reload backend (`--reload` подхватил `engine.py`; иначе перезапустить, чтобы сбросить `SignalProcessor._strategy_cache` с битым кодом) активная сессия #5 на ближайшей 1m-свече SBER в рыночные часы должна открыть `buy`-сделку. Если сделки нет — проверить находку (3) (поступление свечей в поток).

- **BUG-21 ✅ FIXED 2026-06-04 (S8R-ACCEPTANCE-FIX-BUG-21) | severity: HIGH (блокер всех POST/PATCH/DELETE) | `backend/app/auth/router.py:131` | После login любой mutating request падал с 403 («Request failed with status code 403» в UI; в консоли: `POST http://localhost:8000/api/v1/trading/sessions 403 (Forbidden)`). Запуск sandbox-сессии, изменение настроек, удаление — всё блокировалось.** Корневая причина: cookie `csrf_token` выставлялась с `path="/api"`, а `document.cookie` возвращает скриптам только cookies с path = префикс URL текущей страницы. На странице `/trading` cookie с `path=/api` невидима через JS → `getCSRFToken()` в `client.ts:30` возвращал null → header `X-CSRF-Token` не отправлялся. Browser cookie всё же слал (path /api matches request path /api/v1/trading/sessions) → CSRFMiddleware видел «cookie без header» → 403 «CSRF token отсутствует». Запрос не доходил до route handler (в structured log нет ни одной строки от приложения между OPTIONS 200 и POST 403). Раньше работало случайно: max_age=24h, при истечении frontend `getCSRFToken()` возвращал null, browser cookie тоже не слал → middleware пропускал. Свежий login после restart_dev оживил cookie → блок включился. **Фикс**: `path="/api"` → `path="/"` для `csrf_token` cookie. Cookie теперь видна в `document.cookie` со страниц `/trading`, `/chart`, `/strategies`. `access_token` cookie (`httponly=True`, не читается JS) с path="/api" не трогал — она нужна только AdminAuthASGIMiddleware. **Тесты**: новый `test_login_csrf_cookie_has_root_path_bug21` + полный regression auth/csrf **32/32 GREEN** (`test_routers/test_auth_router.py` 14 + `test_security/test_csrf.py` 13 + `test_middleware/test_csrf.py` 5). **UI-верификация**: ОБЯЗАТЕЛЬНО **logout → login заново** (старый cookie с path=/api остался в браузере, новый с path=/ — это другой cookie по ключу `name+domain+path`, не overwrite). Альтернатива: DevTools → Application → Cookies → удалить csrf_token вручную. После этого POST /trading/sessions должен пройти.

- **BUG-20 ✅ FIXED 2026-06-03 (S8R-ACCEPTANCE-FIX-BUG-20) | severity: HIGH (privacy/UX — утечка данных между пользователями) | `frontend/src/components/charts/FavoritesPanel.tsx:16`, `frontend/src/stores/marketDataStore.ts:177` | Заказчик зашёл под новым тестовым пользователем (без избранных), открыл график торговой сессии, нажал звёздочку — увидел чужие тикеры `LKOH`, `SBER`, которые добавлял другой пользователь на этом же устройстве. То же касается избранных таймфреймов в `TimeframeSelector`.** Корневая причина: оба списка хранились в `localStorage` под **глобальными** ключами без user_id-префикса (`user_favorites`, `favoriteTimeframes`), и `authStore.logout()` их не очищал — поэтому новый юзер видел избранное предыдущего. Bonus-проблема: на другом ПК избранное у того же юзера было пустое. **Фикс**: новый backend-модуль `app/user_favorites/` (модель `UserFavorite(user_id, kind, value)`, UNIQUE(user_id, kind, value), 3 REST endpoint'а под `Depends(get_current_user)`) + Alembic миграция. Frontend: `userFavoritesApi.ts`, `userFavoritesStore.ts` (in-memory, **не persist**), переключены `FavoritesPanel.tsx` и `marketDataStore.favoriteTimeframes` на backend. `authStore.logout()` теперь вызывает `useUserFavoritesStore.reset()` + удаляет legacy ключи. `pages/ChartPage.tsx` грузит избранное на mount. **Тесты**: backend `tests/test_routers/test_user_favorites_router.py` — 11 тестов (главный: cross-user isolation `test_user_a_favorites_invisible_to_user_b`); frontend `FavoritesPanel.test.tsx` — 5 тестов (включая регрессию «не читает legacy localStorage»). Quality gates: pytest **1649 passed**, vitest **597 passed**, tsc clean. **UI-верификация**: после логина под новым тестовым пользователем (после `Cmd+R` для очистки in-memory state) звезда «Избранное» должна показать пустой список «Добавьте инструменты»; favoriteTimeframes по дефолту `[D, 1ч, 5м]` (если backend пустой).

- **BUG-18 ✅ FIXED 2026-06-02 (S8R-ACCEPTANCE-FIX-BUG-18) | severity: HIGH (нарушение детерминизма бэктестов на 1+ год) | `backend/app/market_data/service.py:33,78-85` | Два идентичных бэктеста #38 и #39 (SBER 1h, 2024-12-31 → 2026-05-31, capital 300000, identical strategy version 92) дали РАЗНЫЕ P&L: #38 +4103.49 ₽ (15W/23L, Sharpe 0.44), #39 +1640.61 ₽ (14W/24L, Sharpe 0.19). 37 из 38 сделок идентичны.** Корневая причина: `get_candles` (строка 78-85) делал silent clamp `to_dt = from_dt + MAX_REQUEST_SPAN` если диапазон > 365 дней. Период #38/#39 = 517 дней > 365 → `to_dt` обрезался до `2025-12-31`. Engine получал только 12 месяцев свечей вместо 17. Backtrader auto-close открытой позиции в конце урезанного диапазона: для #38 (до BUG-17 fix) → dtclose=`now()=2026-06-02`, exit_price=325.00 (live SBER price), P&L=+2288.28; для #39 (после BUG-17 fix) → exit_date=`2025-12-31`, exit_price=300.02, P&L=-210.21. Один и тот же entry (302.12 на 2025-11-10) дал противоположные результаты. **Фикс**: MAX_REQUEST_SPAN поднят с 365 дней до 3650 дней (10 лет). Защита от случайного огромного запроса сохраняется, но реальные acceptance-периоды (1-2 года) больше не урезаются. T-Invest API сам chunks запросы (1h → 1 год, 1m → 7 дней), а `_find_gaps + _fetch_candles` делает повторные fetches для покрытия gaps. **Тесты**: `TestGetCandlesMaxSpanBug18` × 2 (`MAX_REQUEST_SPAN >= 2 года`, `to_dt не clamped для 17-месячного span`). Полный regression `tests/unit/test_market_data/` — **93/93 GREEN**. **UI-верификация**: testuser1 после `Cmd+R` запускает новый бэктест с теми же параметрами — должен получить **полные 38 сделок** с детерминированным результатом (повторный запуск даёт идентичные метрики).

- **BUG-17 ✅ FIXED 2026-06-02 (S8R-ACCEPTANCE-FIX-BUG-17) | severity: medium (UX cosmetic + CSV/PDF data integrity) | `backend/app/backtest/service.py:_save_result` | На графике аналитики бэктеста последняя сделка (#422 в backtest #38: long, entry 2025-11-10 10:00, exit 2026-06-02 14:00) не подсвечена цветной зоной между entry/exit маркерами. Все остальные сделки имеют корректные profit/loss-зоны.** Корневая причина: Backtrader auto-closes открытые позиции в конце прогона; `bt.num2date(trade.dtclose)` возвращает значение за пределами `date_to` бэктеста (`2026-05-31`). В БД `backtest_trades.exit_date='2026-06-02 14:00'` — после `date_to`. Frontend `computeChartZones` ([tradeMarkerUtils.ts:99](Develop/frontend/src/utils/tradeMarkerUtils.ts#L99)) сматчивает entry/exit маркеры по таймстампам, но exit-маркер вне видимого диапазона свечей не имеет координаты → зона между entry и exit не отрисовывается. **Фикс**: `_save_result` clamp `exit_date` к `backtest.date_to`, если `exit_date > date_to`. Это гарантирует корректность для UI (зона рисуется до конца графика), CSV-экспорта и PDF-отчёта. **Тесты**: `TestSaveResultClampsExitDateBug17` × 2 (exit_date_after_date_to → clamped, exit_date_within_period → unchanged). Полный regression backtest module **215/215 GREEN**. **UI-верификация**: после фикса заказчик должен **повторно запустить** бэктест (rerun #38 или новый), и зона последней сделки появится. Существующий backtest #38 с `exit_date='2026-06-02'` ретроактивно не лечится — но новые сделки будут корректны.

- **BUG-16 ✅ FIXED 2026-06-02 (S8R-ACCEPTANCE-FIX-BUG-16) | severity: high (блокер acceptance Сценария 1 для sandbox) | `backend/app/market_data/service.py` (4 callsites: `_has_active_tinvest_account`, `_fetch_via_broker`, `_get_tinvest_token_for_X` × 2) | Запуск бэктеста на T-Invest **sandbox**-токене падал с `TInvestRequiredError("Подключите T-Invest для запуска бэктеста")`, хотя sandbox корректно добавлен (`tinvest_connected sandbox=True` в логе).** Корневая причина: фильтр `BrokerAccount.is_sandbox == False` в 4 местах `market_data/service.py` исключал sandbox-аккаунты — что неверно, т.к. T-Invest sandbox имеет полный доступ к `MarketDataService` (GetCandles, GetLastPrices, GetTradingStatus). Sandbox отличается только в OrdersService (для live-торговли). **Фикс**: убран `is_sandbox == False` из всех 4 фильтров; добавлен `ORDER BY is_sandbox.asc()` чтобы production-токен предпочитался при наличии обоих (production=0 < sandbox=1). Обновлены misleading-комментарии («sandbox не имеет MarketDataService» → «sandbox даёт MarketDataService»). **Тесты**: `TestHasActiveTInvestSandboxBug16` × 3 (sandbox→True, production→True, no account→False). Полный regression: backend pytest **343/343 GREEN** (auth + backtest + market_data + xss + exceptions). **UI-верификация**: testuser1 после добавления sandbox-токена должен успешно запустить бэктест #36 rerun → метрики появятся.

- **BUG-14 ✅ FIXED 2026-06-02 (S8R-ACCEPTANCE-FIX-BUG-14) | severity: high (UX-блокер + маскировка причины) | `backend/app/common/exceptions.py` (все 7 framework-исключений), `backend/app/backtest/service.py:create_backtest` | На /backtests/36 (SBER, 1ч, кап. 300000) бэктест за 6 мс получил статус «ОШИБКА» с пустым `error_message`. UI показал «ОШИБКА» без пояснения — пользователь не понимает что делать.** Корневая причина: `testuser1` (id=4) не подключил T-Invest. `MarketDataService.get_candles` корректно кидает `TInvestRequiredError(detail="Подключите T-Invest...")`. НО `TInvestRequiredError.__init__` (как и другие 6 классов в `exceptions.py`) сохранял `self.detail`, не вызывая `super().__init__(detail)` — `str(e)` возвращал пустую строку. `BacktestService.create_backtest` ловил всё через `except Exception as e: error_message = str(e)` → в БД писалась пустая строка. Дополнительно: TInvestRequiredError глоталась generic'ом, FastAPI-handler 409 не срабатывал, frontend получал 200 + failed-orphan вместо модалки «Подключите T-Invest». **Фикс**: (1) во ВСЕ 7 классов `exceptions.py` добавлен `super().__init__(detail)` — `str(e)` теперь возвращает detail; (2) `create_backtest` ловит `TInvestRequiredError` отдельно ДО generic except — помечает backtest как failed с осмысленным detail и `raise` пробрасывает наружу → FastAPI handler возвращает 409 + JSON `{detail, error_code: tinvest_required, mode: backtest}`. **Тесты**: 7 новых в `TestExceptionStrBug14` (str(e) == detail для всех 7 классов) + 1 в `TestRunBacktestTInvestRequiredBug14`. Backend pytest 259/259 GREEN (auth + backtest + security regression). **UI-верификация**: после фикса testuser1 при попытке запустить бэктест должен увидеть модалку «Подключите T-Invest для запуска бэктеста» с переходом в настройки брокера.

- **BUG-13 ✅ FIXED 2026-06-02 (S8R-ACCEPTANCE-FIX-BUG-13) | severity: high (потеря работы пользователя) | `frontend/src/pages/StrategyEditPage.tsx:handleGenerate`, `frontend/src/App.tsx:52-53` | На `/strategies/new` после сборки блоков + наполнения описания нажатие «Генерировать» возвращает Python-код, но Blockly workspace и текстовое описание визуально стираются.** Корневая причина: `App.tsx` объявляет ДВА разных Route с одним и тем же элементом — `strategies/new` и `strategies/:id`. React Router при переходе с одного на другой делает unmount → mount; все `useState`/`useRef` сбрасываются. В `handleGenerate` else-ветке (новая стратегия) последовательность была: `createStrategy(name, description)` → `generateCode(id, apiBlocks)` → `setCode(code)` → `navigate(/strategies/${id})`. Но `createStrategy` НЕ пишет blocks_json/generated_code в БД, `generateCode` тоже не пишет — после navigate новая instance читает пустую v1 из БД (`blocks_json='{}'`, `text_description=''`), useEffect перезаписывает workspace пустотой. **Фикс**: в else-ветке `handleGenerate` после `generateCode` и ДО `navigate` вызывается `saveVersion(newStrategy.id, { text_description, blocks_json, generated_code, parameters_json })` — тот же паттерн, что в `doSave` для `isNew`. После remount данные подтягиваются из БД целые. **Тесты**: vitest 8/8 GREEN (existing StrategyEditPage suite), tsc 0 errors. Manual smoke-проверка ожидается заказчиком. **Trade-off**: альтернатива «объединить две Route» отброшена — инвазивнее, риск других регрессий.

- **BUG-12 ✅ FIXED 2026-05-29 (S8R-ACCEPTANCE-FIX-BUG-12) | severity: high (functional blocker для Сценария 1) | `backend/app/auth/service.py:register`, `backend/app/auth/router.py:setup`, `frontend/src/pages/SetupPage.tsx` | POST /auth/setup с уже существующим username возвращает 500 + сломанные CORS-заголовки, UI показывает «Ошибка при создании аккаунта» + DevTools — «Origin not allowed by Access-Control-Allow-Origin».** Корневая причина: `service.register()` делал `await self.db.commit()` без try/except, SQLAlchemy кидал `sqlite3.IntegrityError: UNIQUE constraint failed: users.username`, exception доходил до глобального handler → 500. CORSMiddleware не вешает заголовки в exception path (тот же класс, что у BUG-2 sparkline) → браузер маскирует ответ как CORS error. **Фикс**: (1) `service.register` ловит `IntegrityError` и кидает `ValueError("username_taken")`; (2) `router.setup` ловит `ValueError("username_taken")` и кидает `HTTPException(409, "Имя пользователя уже занято")`; (3) `SetupPage` различает status 409 и показывает осмысленное сообщение из `data.detail`. **Тесты**: 2 новых regression (`test_register_duplicate_username_raises_value_error_bug12`, `TestSetupConflict.test_setup_duplicate_username_returns_409_bug12`). Backend 37/37 GREEN (auth/service + router + xss + cli), tsc 0 errors. **Smoke**: `curl POST /auth/setup` с занятым именем → `HTTP/1.1 409 Conflict` + `access-control-allow-origin: http://localhost:5173` (CORS headers на месте, browser больше не маскирует).

- **BUG-11 ✅ FIXED 2026-05-29 (S8R-ACCEPTANCE-FIX-BUG-11) | severity: low (cosmetic, ложный alarm) | `frontend/src/stores/authStore.ts:logout`, эффект на `frontend/src/pages/LoginPage.tsx:34-44` | После logout LoginPage показывает «Backend недоступен. Убедитесь, что сервер запущен на порту 8000.», хотя backend работает. Перезагрузка страницы убирает сообщение.** Корневая причина: `authStore.logout()` вызывает `abortAllInflight()` (отменяет глобальный module-level `AbortController` в `api/client.ts`), но НЕ пересоздаёт контроллер (в отличие от `login()`, где `renewAbortController()` есть). Router редиректит на `/login`, LoginPage `useEffect` запускает `apiClient.get('/auth/setup-status')`, request interceptor (`client.ts:55-57`) привязывает запрос к уже abort'нутому `inflightController.signal` → запрос мгновенно отменяется `ECANCELED` → `.catch(() => setBackendError(true))`. F5 фиксит, потому что модуль `client.ts` перезагружается с новым неабортнутым контроллером. **Фикс**: одна строка — `renewAbortController()` после `abortAllInflight()` в `logout()` (симметрия с `login()`). Глобальный эффект: все запросы после logout (не только LoginPage) получают свежий signal. **Тесты**: 1 новый regression `renews AbortController on logout (BUG-11)`, обновлён cleanup-order тест (closeWS → abort → **renew** → state → clearCache). vitest 13/13 GREEN, tsc 0 errors. **UI-верификация**: ожидается после Cmd+R.

- **BUG-10 ✅ FIXED 2026-05-27 (S8R-ACCEPTANCE-FIX-BUG-10) | severity: medium (multi-user readiness) | `backend/app/trading/router.py:list_sessions`, `backend/app/trading/service.py:get_sessions`, `backend/app/trading/schemas.py:SessionResponse`, `frontend/src/pages/admin/AdminLandingPage.tsx`, `frontend/src/api/tradingApi.ts`, `frontend/src/api/types.ts` | Карточка «Активные торговые сессии» в Admin Landing показывает сессии только текущего пользователя, а не всех (что ожидается от admin-карточки).** Endpoint `GET /api/v1/trading/sessions` фильтрует через `user_id=current_user.id`. **Фикс**: добавлен query-флаг `?all_users=true` с admin-gate (`HTTPException(403)` для не-админа); service делает batch-JOIN через StrategyVersion+Strategy и возвращает `user_id` в каждом item'е; `SessionResponse.user_id: int | None = None`; AdminLandingPage вызывает `tradingApi.getSessions({ status: 'active', all_users: true })` и показывает колонку User (`#${user_id}`). **Тесты**: 4 новых HTTP-теста (`test_list_sessions_default_filters_by_current_user_bug10`, `test_list_sessions_all_users_requires_admin_bug10`, `test_list_sessions_all_users_admin_sees_everyone_bug10`, проверка наличия `user_id` в response). **UI-верификация**: 2026-05-27 — колонка User видна, значения `#1` для текущего пользователя.

- **BUG-9 ✅ FIXED 2026-05-27 (S8R-ACCEPTANCE-FIX-BUG-9) | severity: high (functional blocker) | `backend/app/middleware/security_headers.py`, путь `/api/v1/admin/metrics/*` | Plotly Dash рендерит только статичный «Loading…» из-за CSP, блокирующей inline-скрипты Dash.** Глобальный `SecurityHeadersMiddleware` выставлял `Content-Security-Policy: default-src 'self'; frame-ancestors 'none'`. **Фикс**: path-based exception в middleware — для `request.url.path.startswith('/api/v1/admin/metrics')` отдаётся `_PLOTLY_DASH_CSP = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; frame-ancestors 'none'"`. Остальные эндпоинты остаются под строгим `_DEFAULT_CSP`. **Тесты**: 3 новых regression — `test_admin_metrics_returns_relaxed_csp`, `test_admin_metrics_subpath_returns_relaxed_csp`, `test_default_csp_still_strict_for_other_paths`. Все 9/9 GREEN. **UI-верификация**: 2026-05-27 — 4 графика Plotly Dash рендерятся, console clean.

- **BUG-8 ✅ FIXED 2026-05-27 (S8R-ACCEPTANCE-FIX-BUG-8) | severity: high (functional gap + security design) | `backend/app/auth/router.py`, `frontend/src/pages/admin/AdminLandingPage.tsx`, `frontend/src/api/client.ts` | Ссылка на Plotly Dash `/api/v1/admin/metrics` в Admin Landing открывает SPA-404, при попытке прямого доступа backend возвращает 401.** Два слоя: (8a) href относительный → Vite SPA отдаёт index.html (нет proxy для `/api`), React Router показывает 404. (8b) Даже с абсолютным href на `:8000` backend возвращает 401: `AdminAuthASGIMiddleware` ждёт cookie `access_token`, но login сетит только `csrf_token`. **Фикс**: backend `_set_access_token_cookie()` helper выставляет `access_token` как HttpOnly cookie (`samesite='lax'`, `path='/api'`, `max_age=JWT_ACCESS_TOKEN_EXPIRE_MINUTES*60`) на `/setup`, `/login`, `/refresh`; `/logout` чистит cookie. Frontend axios с `withCredentials: true`. AdminLanding `PLOTLY_DASH_URL` строится через `VITE_API_BASE_URL` + trailing slash. **UI-верификация**: 2026-05-27 — ссылка открывает Plotly Dash в новой вкладке без 401/404.

- **BUG-7 ✅ FIXED 2026-05-27 (S8R-ACCEPTANCE-FIX-BUG-7) | severity: low | `backend/app/cli/users.py` | CLI `grant_admin` падал с `sqlalchemy.exc.InvalidRequestError: expression 'Strategy' failed to locate a name` ещё до выдачи прав.** Корневая причина: модуль импортировал только `User`, но у `User` есть relationship на `Strategy` через `relationship('Strategy', ...)` — SQLAlchemy mapper на init не мог разрешить ссылку, потому что `Strategy` ещё не зарегистрирован. **Фикс**: скопирован паттерн «register all models» из `app/main.py` — добавлены `from app.{auth,strategy,backtest,trading,broker,market_data,notification,circuit_breaker,corporate_actions,tax,common} import models as _* # noqa: F401` перед `from app.auth.models import User`. **Дополнительно**: в чеклисте (`Сценарий 5`) была неточная команда `python -m app.cli grant_admin ...` — поправлено на корректную `python -m app.cli.users grant_admin ...` (модуль `app.cli` — package без `__main__.py`, исполняется именно `app.cli.users`). **Верификация**: `python -m app.cli.users grant_admin sergopipo` → `User 'sergopipo' is now admin`, SQL UPDATE проверен. Тэг: `S8R-ACCEPTANCE-FIX-BUG-7`.

- **BUG-1 ✅ FIXED 2026-05-14 (Sprint 8 W7) | severity: lethal | `app/trading/engine.py`, `app/trading/runtime.py` | Sandbox/real торговля не была реализована в `engine.process_signal`. Trade создавался со `status=pending`, `TInvestAdapter.place_order` никем не вызывался. Реализовано в W7 (Вариант C++): `_submit_order_to_broker` отправляет market-order и резолвит trade по `OrderResponse.status`; `_recover_orphan_pending_trades` зачищает orphan'ы при старте backend. 13 новых тестов (8 sandbox-flow + 5 orphan-recovery), backend regression 1560/0 passed. ФТ v2.5 → v2.6.** Ожидается live-test заказчиком для финального подтверждения.

- **BUG-6 ✅ FIXED 2026-05-25 (S8R-ACCEPTANCE-FIX-BUG-5+6, в одном коммите с BUG-5) | severity: high (security) | `backend/app/tax/service.py:103-119` (`_load_trades`) | `GET /api/v1/tax/reports/{id}/download` НЕ фильтровал сделки по `user_id`.** В запросе `select(LiveTrade).join(TradingSession).where(LiveTrade.status == "closed", ...)` отсутствует `Strategy.user_id == user_id` (LiveTrade → TradingSession → StrategyVersion → Strategy → user_id). При появлении второго пользователя его сделки попадут в чужие налоговые отчёты. Single-tenant сейчас не страдает (один юзер), но это data-isolation дефект, обязательный к фиксу до production. **Тэг**: `S8R-ACCEPTANCE-FIX-BUG-6` (фиксится вместе с BUG-5).

- **BUG-5 ✅ FIXED 2026-05-25 (S8R-ACCEPTANCE-FIX-BUG-5+6) | severity: high | `backend/app/tax/service.py:121-225` (`_build_fifo_queue`) | Содержимое xlsx/csv налогового отчёта всегда было пустое — все агрегаты (total_profit, total_loss, taxable_base) = 0, в лотах exit_price/realized_pnl = NULL.** Корневая причина: алгоритм FIFO matching рассчитан на модель «отдельные buy- и sell-leg'и» (для каждой sell-сделки найти самый ранний открытый buy-лот). В этом коде LiveTrade — другая модель: одна строка = **уже закрытая пара entry+exit** (поля `entry_price`, `exit_price`, `opened_at`, `closed_at`, `pnl` — всё в одной записи). Поле `direction` хранит направление позиции (long/short), а не buy/sell-leg. В БД у заказчика 7 closed `live_trades` с `direction='buy'` (long), 0 с `direction='sell'`. Алгоритм: → buys.append(×7), sells.append(×0) → 7 open_lots, 0 sells для matching → ни один лот не закрыт → `realized_pnl=NULL` у всех 14 tax_lots → агрегаты 0. Воспроизведение: любой закрытый paper-трейд → POST `/tax/reports` → GET `/tax/reports/{id}/download` → файл с шапкой, без строк. **Фикс**: переписать `_build_fifo_queue` под closed-pair модель — для каждой LiveTrade.status='closed' создать TaxLot с rebuilt из entry+exit полей PnL = `(exit_price - entry_price) * filled_lots` (для long) или обратно (для short), минус commission. Заодно — фикс BUG-6 в одном коммите. **Тэг**: `S8R-ACCEPTANCE-FIX-BUG-5`. **UI-верификация заказчика 2026-05-25**: перегенерирован отчёт 2026 → в xlsx 7 строк с реальными ценами и PnL (SBER + LKOH paper-сделки), агрегаты total_profit/loss/taxable_base заполнены корректно. См. `Спринты/Sprint_8/changelog.md` запись `2026-05-25` для деталей рефакторинга + 9 новых тестов.

- **BUG-4 ✅ FIXED 2026-05-23 (S8R-ACCEPTANCE-FIX-BUG-4) | severity: medium | `frontend/src/components/account/TaxReportModal.tsx`, `frontend/src/stores/accountStore.ts`, `frontend/src/pages/AccountPage.tsx` | Кнопка «Сгенерировать отчёт» создавала файл на бэкенде, но в UI ничего не происходило — файл не скачивался и не отображался.** Корневая причина: `downloadTaxReport` action и `taxApi.downloadReport` существовали, но **нигде в UI не вызывались** (grep по всем .tsx — 0 хитов). Список `taxReports` тоже нигде не отрисовывался. **Фикс**: (a) `generateTaxReport` теперь возвращает `Promise<TaxReport>` (раньше void) — чтобы UI знал id новой записи; (b) `downloadTaxReport(id, filename?)` — опциональный кастомный filename; (c) на `AccountPage` добавлена обёртка `handleGenerateAndDownload`, которая после `generateTaxReport` сразу запускает `downloadTaxReport(id, "tax_report_${year}.${format}")`; (d) на mount `AccountPage` тянет `fetchTaxReports` и отрисовывает новую секцию «Налоговые отчёты» — таблица с колонками год/статус/дата/налоговая база и кнопкой-иконкой `IconDownload` на каждой строке. **Тесты**: 9/9 AccountPage GREEN, 7/7 accountStore GREEN (+2 новых для BUG-4), 2/2 TaxReportModal GREEN, `npx tsc --noEmit` 0 errors. **UI-верификация заказчика 2026-05-23**: список отчётов отображается, генерация → авто-скачивание работает, кнопка «Скачать» на существующих строках срабатывает. Корневая причина: пропущена интеграция UI. Backend работает (POST `/tax/reports` → файл `data/tax_reports/tax_report_1_2026.xlsx` создаётся, status=ready; GET `/tax/reports/{id}/download` отдаёт blob корректно). На фронте есть `taxApi.downloadReport` и `accountStore.downloadTaxReport` (Blob → click), но **`downloadTaxReport` нигде в UI не вызывается** (grep по всем .tsx — 0 хитов). Модалка после `onGenerate` сразу закрывается (`onClose()`), а отрисовки списка `taxReports` на AccountPage нет. Воспроизведение: Account → «Налоговый отчёт» → выбрать год 2026 → «Сгенерировать отчёт» → модалка закрылась, файл не скачался. Подтверждено в логе backend: POST 200 + GET 200 (список), но **нет** GET `/download`. **Тэг для фикс-ветки**: `S8R-ACCEPTANCE-FIX-BUG-4`.

- **BUG-3 ✅ FIXED 2026-05-20 (S8R-ACCEPTANCE-FIX-BUG-3 + followup) | severity: medium | `app/account/service.py`, `app/account/schemas.py`, `BalanceWidget.tsx`, `accountApi.ts` | «За день» в виджете Balance показывал фейковый -27% / -80 961 ₽ из-за методологической рассинхронизации формул.** Корневая причина: today считался через `sum(PaperPortfolio.balance)` (только cash, без sandbox, без позиций), прошлые дни — через `initial_capital + cumulative_realized_pnl`. **Фикс**: (a) снят today-special-case в `get_balance_history` — единая формула на все дни; (b) добавлено поле `trading_pnl: float` в `BalanceHistoryPoint` schema = `cumulative_realized_pnl(<=d)` без `initial_capital`; (c) `BalanceWidget` теперь использует `trading_pnl` для sparkline и дневной дельты (а `total_value` — только для большой цифры), чтобы открытие новой сессии не выглядело как «торговый рост»; (d) UX-полировка: при ровно 0 дневной дельте — нейтральный state (`IconMinus`, dimmed-текст, без стрелки); цвет sparkline теперь трёхзначный (green/red/`gray-5`). **Тесты**: +1 новый regression-тест `test_balance_history_today_and_yesterday_use_same_formula_bug3` + 1 для `trading_pnl` separation; 13/13 backend GREEN, 6/6 frontend BalanceWidget GREEN, `npx tsc --noEmit` 0 errors. **Trade-off**: sparkline теперь — «P&L curve», а не «balance over time»; реальная equity-кривая с market value позиций — задача Sprint 9 (`S9-EQUITY-DAILY-SNAPSHOT`). См. `Спринты/Sprint_8/changelog.md` запись `2026-05-20`. UI-верификация заказчиком: 299 891 ₽ + «+0 ₽ серым» + красная sparkline со ступенькой -109.16 (закрытие LKOH на 2026-05-08).

- **BUG-2 | severity: medium | `app/market_data/service.py:168-188` (`_purge_iss_cache`) → `router.py:79` (`get_sparkline`) | Transient HTTP 500 при первом обращении к `/api/v1/market-data/sparkline` на холодном старте backend.** Trace в backend.log: `sqlite3.OperationalError: database is locked`. Корневая причина — конкуренция за SQLite writer-lock между lifespan startup prefill (DELETE из `ohlcv_cache` для прогрева SBER, политика S5R «T-Invest — единственный источник») и параллельным API-запросом с frontend, который через `_purge_iss_cache` запускает аналогичный DELETE. Существующий retry на 3 попытки (S5R hotfix) не всегда спасает при startup-конкуренции. После прогрева — все запросы 200 OK (подтверждено логом `12:46:59 ... 200 OK`). **Симптом в DevTools**: красная строка `Origin http://localhost:5173 is not allowed by Access-Control-Allow-Origin. Status code: 500` — это маскировка от браузера; реальная причина 500, не CORS. **Приоритет фикса**: medium — не блокирует UX (sparkline появится при перезагрузке страницы), но шумит в Console и портит впечатление. Варианты фикса: (a) WAL `busy_timeout` повысить, (b) ретраи увеличить с 3 до 5-7 + экспоненциальный backoff, (c) отложить prefill до окончания startup через `lifespan` deferred-task.

---

## Общие замечания

> Сюда пиши любые мысли, идеи, неудобства UX, странности поведения, которые не тянут на «баг», но требуют обсуждения.

- **DevTools-шум на dev-сборке (некритично, но мешает приёмке):**
  - `WebSocket ... closed before the connection is established` — артефакт React 18 StrictMode (двойной mount компонента в dev → отмена первого WS-соединения). В production-сборке `pnpm build && pnpm preview` отсутствует. Если хочется чистый Console при приёмке — можно либо отключить StrictMode в `main.tsx` на время review, либо договориться, что dev-only ошибки игнорируются.
  - **React HTML-warning**: `<p> cannot contain a nested <div>` (frontend log) — Mantine `Text` (рендерится в `<p>`) содержит вложенный `Group` (`<div>`). Невалидный HTML, но без визуального дефекта. Завести задачу на починку (`component="div"` или `span="true"` для `Text`-обёртки) в S9 backlog.
  - React Router v7 future-flag warnings (`v7_startTransition`, `v7_relativeSplatPath`) — желательно опт-ин до миграции на v7.
- **T-Invest CDN logo gracefully fallback**: для облигации `RU000A10DS74` (Сибур-Холдинг 001P-08) Tinkoff CDN отдаёт 403. Фронту стоит подавлять Console-ошибку через `<img onError>` и показывать дефолтную плашку (буква/иконка). Не баг функциональности, но шум в Console.

---

## Что показалось хорошим

> Сюда — фичи, которые работают как ожидалось или приятно удивили. Не пропусти этот раздел — он важен, чтобы не потерять то, что уже хорошо.

-

---

## Финальный вердикт

После прохождения всех шагов выбери один:

- [ ] **PASS** — все 136 пунктов зелёные, 0 багов severity ≥ medium. Sprint_8_Review закрыт, готов к старту Sprint 9 "Перевод в продуктив".
- [ ] **PASS WITH NOTES** — есть N багов severity ≤ medium, либо закрываем их в Sprint 8 (как W4/W5), либо переносим в Sprint 9 backlog. Sprint_8_Review закрыт.
- [ ] **NEED FIXES** — ≥1 lethal/critical баг. Стоп до фикса. Список багов передан, ждём моей реализации фиксов в `s8/sprint-8` ветке, после фикса возвращаемся к acceptance.

**Краткий итог (1–2 предложения):**

>

**Дата подписи:** _(заполни в конце)_

---

## Что дальше после Acceptance

1. Сохрани файл с заполненными чекбоксами и заметками.
2. Скажи мне «Acceptance закончен, посмотри файл» — я прочитаю.
3. По багам severity lethal/critical → фиксим сразу в `s8/sprint-8` ветке (тэг `S8R-ACCEPTANCE-FIX-*`).
4. По багам severity medium/low → решаем вместе: закрыть в S8 или перенести в Sprint 9 backlog.
5. Когда вердикт PASS или PASS WITH NOTES → закрываем Sprint_8_Review, стартуем **Sprint 9 "Перевод в продуктив"** (Mac mini Docker prod → canary → автоматизация).
